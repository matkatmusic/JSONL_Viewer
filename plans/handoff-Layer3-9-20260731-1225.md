# Handoff: orchestrating subagents through the Layers 3–9 task backlog (tasks 332–371)
Conversation name: L3-9 tasks round 1
JSONL: /Users/matkatmusicllc/.claude/projects/-Users-matkatmusicllc-Desktop-claude-code-src-RevEng/7fa9beca-a3ff-42e1-a182-bc136134bbf3.jsonl
Plan file: plans/340-recon/DECISIONS.md (cross-cutting L3–L9 decisions; the authority for everything below)

## Branch
RevEng `Layer3-9` based on `master`. jfred submodule also on `Layer3-9`.
Side branch `tasks-345-353` exists in RevEng, **4 commits ahead of Layer3-9, unmerged.**

## Goal
Work the Layers 3–9 backlog for task #340 by orchestrating subagents rather than
implementing directly — the user explicitly asked for "farm out to sub agents,
you are the orchestrator". Tasks come from `tasks.json`; the `taskTools:tackle-tasks`
skill drives plan → implement → typecheck → merge through the workflow script at
`/Users/matkatmusicllc/Programming/taskTools/skills/tackle-tasks/tackle-tasks.workflow.js`.

## Current State

**Closed this session — TEN tasks:** 345, 348, 353, then 332, 333, 334, 336, 339,
346, 347. All moved to `completedTasks.json` with real commit hashes. Closures
unblocked **356**, **357** and **358**.

`tasks.json` is down to 52 open tasks.

**Committed:** jfred `811966e` (wash lifecycle fixes, `.mm-wash` rule, blue nav
wash), taskTools (ported tests), RevEng `f4c8527` (decision docs, test deletions,
handoff, jfred pointer), `eee4be5` on the side branch (mockup rebuild), and
`2f5f8ed` merging `tasks-345-353` into Layer3-9. The side branch is fully absorbed
— 0 commits ahead.

**Uncommitted, staged, NOT yet committed:**
- jfred: `tests/layer1-filenav-collapse.test.ts` — a stale assertion fixed during
  the closing gate (see below)
- RevEng: `tasks.json`, `completedTasks.json` (the seven-task closure),
  `specs/from-scratch-SPEC.md` (marked #339 done, pointing at the shipped palette)

**Left untracked on purpose:** `plans/tackle-metrics.jsonl` in both trees (task
tooling's own metrics), the `plans/brief-*.md` and `plans/task-*-plan.md` files,
and `plans/task-338-findings.md` — that last one is task 338's real deliverable,
so it may deserve committing.

**Both suites are green:** jfred 1543/1543 (2 skipped), RevEng 13/13, typecheck
clean in both.

**RevEng test suite is now GREEN: 13 tests, 13 pass, 0 fail** (verified directly).
It was 23/14/9 before this session — the 9 failures came from 5 stale test files
whose subjects were ported out to taskTools in commit 659f8bf. Resolved: the three
orphan tests (`archiveProcessed`, `extractOpenSections`, `nextTaskNumber`) were
ported into `taskTools/tests/`, missing assertions were folded into taskTools'
existing `getTaskDetails`/`viewTaskHook` tests, and all five were deleted from
`RevEng/tests/`. taskTools is 701/701. Both sets of deletions/additions are
UNCOMMITTED in their respective repos.

**`plans/node-kinds-mockup/` exists** on the `tasks-345-353` worktree — one bubble
with all 12 node kinds, a legend, and a separate confidence row proving the
dashed-ring idiom is independent of kind colour. Browser-verified. Task 339
shipped a complete set: every kind it names has a `.node-type-*` rule in
`layered-styles.css@ee4603f`, no gaps.

## What Remains

1. **No subagents are in flight.** All three finished. The L5 mockup was
   restructured in the `tasks-345-353` worktree: `variant1/` (rejected dim-in-place)
   DELETED — recoverable from commit `ec4a08c`, the deletion is uncommitted —
   `index.html` rewritten to state the design is decided, `variant2/` repurposed as
   the code-rewind dead-end case, `variant3/` as the conversation-only rejoin case
   with a new mirrored `renderMergeElbow`. Fixtures switch on a `rewindKind` field;
   nothing is derived. Browser-verified with real screenshots. The agent also fixed
   a genuine bug: the shared lane-rail helper was putting the solid `.g-rail.g-l1`
   on the branch lane as well as the trunk, contradicting "trunk stays solid, branch
   gets a dashed line".

2. **Show the user `plans/node-kinds-mockup/`** so 339 can get a verdict — this is
   the only thing blocking 339 from closing. Serve with `python3 -m http.server`;
   ES modules do not load off `file://`.

3. **Have the user re-verify the nav wash colour** (now blue `--c-commit`) in the
   browser, then close 332, 333, 334, 336 in ONE `close-tasks` invocation.
   339 closes separately once its mockup is approved.

4. **Merge `tasks-345-353` into Layer3-9** with `--no-ff`. See the trap below.

5. **Close 346 and 347** once their mockups are accepted — that unblocks **357**
   and **358**.

6. **Clean up 5 stale worktrees** at `taskTools-wt/RevEng/group-1` … `group-5`,
   pinned at `2b94b48` on `task-group-N` branches. They were created by a
   `/tackle-tasks` invocation that was superseded; they contain no work.

7. **Tasks 363–371 are new and unstarted** — the user's nine UI bugs/features from
   this session. 365 (aggregate nested git repos into the timeline) is the big one.
   362 (parse workflow-spawned subagent sessions) was filed by an agent on its own
   initiative from task 338's findings; it is legitimate but the user did not ask
   for it.

8. **Task 338 has no fix task yet.** `plans/task-338-findings.md` documents a
   verified gap: file edits inside a `/btw` fork subagent are silently dropped
   because nothing descends into `<sessionId>/subagents/`. Ask before filing.

## Key Files
- `plans/340-recon/DECISIONS.md` — cross-cutting L3–L9 decisions; "Still open" is
  now down to L5 lane scope only
- `plans/340-recon/L5-branches.md` — L5 forked-track decision, settled 2026-07-31
- `plans/340-recon/L6-script-results.md` — L6 popup decision, settled 2026-07-31
- `specs/from-scratch-SPEC.md` — 1179 lines; S21–S24 added this session
- `jfred/webapp/layer1-page.ts` — stage render pipeline; order is
  `renderSessionRanges → renderNavWash → repaintDiffWash → drawLayer1Minimap`
- `jfred/webapp/layer1-wash.ts` — `spawnWash`; sets `--axis-px`, `--span-px`, `--wash-color`
- `jfred/webapp/layer1-styles.css` — `.wash` at ~208, `.mm-*` at ~441
- `/Users/matkatmusicllc/Programming/taskTools/skills/tackle-tasks/tackle-tasks.workflow.js`
  — the orchestration script

## Context the Next Agent Won't Have

**The `files` array in tasks.json is load-bearing and cost this session four
failed runs.** Workers are told "touch nothing outside them" and correctly refuse
when the plan needs a file the grant omits. Three separate rounds produced ZERO
work because the grant was too narrow or named the wrong folder. Always derive
`files` from the plan file on disk, never from the brief and never by guessing.

**Re-running a workflow re-runs the planner, which OVERWRITES the plan file.**
Task 347's plan was rewritten between runs with the opposite folder name and
structure, invalidating a `files` array built from the previous version. Use
`resumeFromRunId` so planners replay from cache and cannot rewrite plans.

**`merged: true` does not mean work happened.** A blocked worker makes no commits,
so its branch merges cleanly and empty. Always verify with
`git diff --stat <base> <branch>` before believing a merge result.

**Git requires a clean INDEX to merge**, even when the incoming files do not
overlap the staged ones. `git merge-tree` will NOT tell you this — it only checks
tree content. The working recipe: `git restore --staged <paths>`, merge, re-stage.

**DOM tests pass while the UI is visibly broken.** Task 334's minimap marks were
built and inserted correctly but had no CSS rule at all, so they were invisible.
The agent that "fixed" 334 never touched the minimap and its DOM assertions all
passed. Honour the CLAUDE.md rule: visual fixes need a real browser.

**`buildFileTree` strips the common prefix, which changes as you filter.** This
broke a task 332 test that searched for a term matching a single file: with one
path surviving, the stripped prefix grew from `src` to `src/nested`, so the
filtered tree rendered that file at root with no folders at all and the assertion
hunted a folder that could not exist. Any File Nav test asserting on folders must
keep at least two files alive across the filter.

**Task 333's nav wash colour was never re-checked in a browser.** It was changed
from `--c-echo` (amber, which clashed with the `--box` bubble colour) to
`--c-commit` (blue) AFTER the user approved 333/334, and they chose to close
anyway. If blue reads wrong, it is a one-token revert in
`webapp/layer1-nav-wash.ts`.

**Check `scenarios/` before claiming the engine lacks something.** The user
corrected this session sharply. A scenario existing means the engine already
handles that case. The DSL marks rewind kind directly: `Rewind: N` is
conversation-only, `Rewind: N, code` is a code rewind, and they exist as twin
pairs s12/s11, s14/s13, s17/s16, s19/s20, s22/s23, s59/s60. Recon docs under
`plans/340-recon/` describe what was unknown *during recon* and go stale — they
are not a statement of engine capability.

**RevEng worktrees spawn with an empty `jfred` submodule and no `node_modules`.**
Run `git submodule update --init jfred` and symlink `node_modules` from the main
checkout, or typecheck fails with `TS2688: Cannot find type definition file for
'node'` and reports a false pass/fail.

**`NODE_OPTIONS` contains a VS Code JS-debug bootloader that hangs `tsc` and
`node --test`.** Prefix every invocation with `NODE_OPTIONS= `.

**User preferences expressed this session:** merges are always `--no-ff`; "merge"
never implies push; nothing gets committed without being asked; regressions in
just-delivered work get fixed before new tasks are filed; the spec's 80-column
wrapping and agent-tuned shorthand grammar make it hard for a human to read —
worth changing for future spec items.

**The `close-tasks` and `tackle-tasks` skill arguments reach the shell inside
single quotes.** Apostrophes, parentheses, and backticks in the reasoning text
break the invocation.

## How to Verify

```
cd "/Users/matkatmusicllc/Desktop/claude code src/RevEng/jfred"
NODE_OPTIONS= npx tsc --noEmit
NODE_OPTIONS= npm test

cd "/Users/matkatmusicllc/Desktop/claude code src/RevEng"
NODE_OPTIONS= npx tsc --noEmit          # clean
NODE_OPTIONS= npm test                  # 23 tests / 14 pass / 9 fail BEFORE the
                                        # stale-test agent lands; expect 14/14 after
```

Browser check for the Layer 1 work:
```
lsof -ti :7343 | xargs kill -9 2>/dev/null
cd "/Users/matkatmusicllc/Desktop/claude code src/RevEng/jfred"
npm run build:webapp && npx tsx src/viewer_server.ts --projects-dir "$HOME/.claude/projects" --port 7343
```
then open `http://127.0.0.1:7343/app/layer1.html` — note the route is `/app/layer1.html`.

Mockups (ES modules need a server, not `file://`):
```
cd "/var/folders/fy/wg2tzrv957sg2vqjcvdkdzvm0000gn/T/taskTools-wt/RevEng/side/plans/l6-console-mockup" && python3 -m http.server 8000
cd "/var/folders/fy/wg2tzrv957sg2vqjcvdkdzvm0000gn/T/taskTools-wt/RevEng/side/plans/layer5-branch-mockup" && python3 -m http.server 8001
```
