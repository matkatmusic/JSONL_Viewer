[x] S1 -> [x] fix types (no primitives) -> [x] commit
[x] S2 -> [x] Read/Edit tools + StructuredPatchHunk + 3 attachment kinds + isMeta gate
[~] Per-line reconstruction engine -> [x] S1 slice (Write create -> rm delete, --verbose/--diff); [ ] Edit/observation/verdict
[x] S3 -> [x] cp copy lineage (CopyEvent + seed-from-source-at-copy-time + first-class copy entry; 3 independent histories)
[x] S4 -> [x] overwrite (2nd Write to a present file = replay-time presence distinction; first-class overwrite revision kind; render-list split; queue-operation record type added)
[x] S5 -> [x] bash redirect (`>>` append = carried-prefix + genesis-suffix revision; `>` overwrite = S4 reuse; content recovered from the file-history sidecar via injected BackupReader, snapshot paths resolved against cwd; replay-edit split; DOES_NOT_EXIST_YET sentinel; render-list verb renames)
[x] S6 -> [x] git mv rename (generalized parseMvPaths to accept `git mv`; cwd-relative rename paths resolved absolute via shared resolveAgainstCwd in new structures/path-resolve.ts leaf module; reuses S2 rename lineage + edit splice; no new event kind, no sidecar)
[x] S7 -> [x] conversation rewind / code restore (branch-aware reconstruction; rewind = a parentUuid fork named by the final last-prompt leafUuid; surviving branch is the engine default, rewound branches PRESERVED & retrievable like unmerged git branches; new reconstruction_branch.ts model + reconstruction_branches.ts core split off engine; reconstructBranches + CLI default-all-branches with --surviving/--list-branches/--branch; no new event/revision/per-line kind)
[x] S8 -> [x] repeated code-restore rewinds + final conversation-only rewind (surviving working tree resolved from file-history-snapshot tracked-set changes, not the final conversation head; new reconstruction_tree.ts walkers + reconstruction_worktree.ts owner detection; findSurvivingHead working-tree-aware; no-op for S1–S7; no new event/revision/per-line/container kind)
[x] S9 -> [x] code restore to older code with no post-edit (surviving working tree is the restored code; findWorkingTreeOwner detects change by content identity — carried-forward backupFileName — not the version counter, so post-restore refresh snapshots don't move the owner; reconstructAll(S9) is the restored write turn, reconstructBranches(S9) has zero rewound branches; refines S8 locked decision #3; single change in reconstruction_worktree.ts, no new module/event/revision/per-line kind, no CLI change)
[x] S10 -> [x] conversation-only rewind with no post-edit (the files written on the abandoned conversation branch stay on disk and ARE the surviving working tree; the read-only post-rewind head is a file-less tangent, so zero rewound branches and a plain single-branch list; no engine change — the isolated conversation-only-rewind case of spec 35, strict no-op for spec 36 since the post-rewind snapshots repeat the same version AND the same non-null backupFileName; locked by tests + spec 37; new tests/reconstruction_engine_s10.test.ts + tests/reconstruction_cli_s10.test.ts + 1 synthetic branch test, no new module/event/revision/per-line kind, no CLI change)
[x] S11 -> [x] code restore then post-rewind rewrite (surviving working tree is the rewritten `multiply` code, recovered from its Write events; the abandoned pre-restore `add` turn wrote files so it is preserved as ONE rewound branch forked at the root checkpoint — `## surviving` + `## rewound` headers, not a plain list; no engine change; locked by tests + spec 38; the complement of S9 — a real rewrite produces a NEW non-null backupFileName at a NEW version, so the content signature changes and the working-tree owner ADVANCES, where S9's null-bfn refresh did not)
[x] S12 -> [x] conversation-only rewind THEN post-rewind edit (first production-code fix + a two-DAG CLI). The surviving branch Edits files whose creating Write is off-branch (on the abandoned rewound branch the conv-only rewind left on disk), so the Edit replays against an empty base and the engine CRASHED in insertHunkAdditions; fixed by seedEditBaseFromBackup (prepend a synthetic Write seeded from the at-or-before file-history backup) + a total context-line branch (resolveContextLine). Adds a two-DAG render: --graphConvo (forks at the rewind, rewound-above-surviving, oldest-first) + --graphFile (linear per-file disk lineage), sharing one letter per turn; NO flags = both graphs, the new GLOBAL default that retires the `## surviving`/`## rewound` text default for every scenario. New modules reconstruction_graph.ts + reconstruction_graph_render.ts + reconstruction_labels.ts + BranchRole enum; specs 39+40; 12 default-view CLI tests rewritten to the graph output; 151 tests green.
[x] S13 -> [x] multi-edit code-restore-read (the S9 analog with an EDITED abandoned branch). A code-restore rewind forks at a system record into an abandoned branch that Reads+Edits scenario13.py to add `farewell` and a surviving branch that only Reads (disk rolled back to the `greet`-only content). The abandoned tip is named by NO last-prompt head (the rewind re-prompted from the fork point), so findConversationBranches orphaned the `farewell` Edit — the fileDAG showed it but the conversationDAG/--list-branches missed the rewound branch. Fix Part 1: discover the rewound branch STRUCTURALLY from the parentUuid fork (a record parenting ≥2 isGenuineUserPrompt children) via new reconstruction_tree.ts walkers (isGenuineUserPrompt/findPromptForkPoints/collectDescendantUuids/findDeepestPromptOrReply) + new leaf module reconstruction_fork.ts (findStructuralRewoundBranches, with a dedup guard that skips abandoned subtrees already holding a head-based tip — protects S7/S8/S11/S12; S9/S10 read-only branches auto-filter via divergingIds.size===0). Fix Part 2: keep a file-less surviving branch visible when a rewound branch exists so the conversationDAG renders the fork honestly (`(no file changes)` marker, empty branch sorts last). No new event/revision/per-line kind; --surviving content + fileDAG unchanged; 172 tests green; S1–S12 byte-for-byte unchanged.
[x] S14 -> [x] multi-edit conv-only-read (the conv-only twin of S13). Same write→edit→rewind→read shape, but the rewind is conversation-only (no `, code`), so disk KEEPS the abandoned `farewell` Edit. Two bugs hid the rewound branch and mis-named the surviving one: (1) findSurvivingHead's working-tree override mis-fired — conv-only leaves `farewell` on disk so findWorkingTreeOwner lands on the ABANDONED branch (`2f3a2bcb`), `finalChain.has(owner)===false`, and the override handed back the abandoned prompt `fadbe55d` as "surviving"; (2) the abandoned prompt `fadbe55d` is itself a `last-prompt` head in S14 (it was not in S13), so the head-based pass claimed it and `subtreeHoldsClaimedTip`'s `claimed.has(abandonedPrompt)` short-circuit blocked structural discovery of the real `farewell` tip `68f74356`. Fix Part 1 (reconstruction_branch.ts): only redirect to the working-tree owner when the final-head branch records NO file change of its own — new survivingBranchRecordsFileChange helper (`extractFileEvents(selectBranchRecords(records, finalHead)).length > 0`) keeps finalHead for S14 while S8/S9/S10 (file-less surviving branch) still redirect. Fix Part 2 (reconstruction_fork.ts): delete the `claimed.has(abandonedPrompt)` short-circuit — collectDescendantUuids excludes `start`, so S7/S8/S11/S12 (claimed head tip is a descendant) stay skipped, only S14's deeper structural tip is discovered. Output is byte-identical to S13 (rewound `#68f74356` above file-less surviving `#de63b23a`, fileDAG B write + D edit unchanged). 8 new tests (4 engine + 4 CLI); 180 tests green; S1–S13 byte-for-byte unchanged. NOTE: findConversationBranches yields the degenerate head-based `fadbe55d` branch too (harmless — no diverging file change, filtered by reconstructBranches), so the engine test asserts the two meaningful branches' presence rather than a raw length===2 (the plan's stated count; see implementation notes).
[x] S15 -> [x] user out-of-band edit then conv-only rewind — opens the user-edit family (S15–S23): a user edits scenario15.py directly on disk, recorded as an `edited_text_file` ATTACHMENT (not a tool_use), then a conv-only rewind abandons that branch for a Read-only branch. New `AttachmentPayloadType.edited_text_file` + `EventKind.userEdit` + leaf module reconstruction_user_edit.ts (`userEditEventFrom`/`stripLineNumberPrefixes`) turn the snapshot into a user-edit file event; the shipped S13 structural fork discovery + S14 surviving-head guard then surface the fork with no further branch/worktree change. CORRECTED a defect in the S15 plan: `edited_text_file` is a disk-snapshot ECHO the IDE emits on every write/read, present in the locked S5/S13 transcripts too (and 9 future scenarios) — not S15-only as the plan claimed. So a content-aware guard records the snapshot as a `user-edit` change ONLY when it differs from the file's current content (per the user's "reconstruct the change history 100%, attribution second" directive): reconstruction_replay.userEditChangesContent drops no-op echoes from content views; reconstruction_branches.collectAcceptedUserEditIds + extractRenderableEvents keep the two graphs in sync; the CLI passes its sidecar reader into renderGraphs so the fileDAG is content-correct for the bash-redirect lineage. 17 new tests; 197 green; S1–S14 byte-for-byte unchanged (zero user-edit turns); S15 shows the genuine edit as a rewound `user-edit` turn above a file-less surviving Read branch + its fileDAG turn.
[x] S16 -> [x] multi-edit code-restore then post-restore RE-EDIT (the re-edit twin of S13). The code restore (`Rewind: 2, code`) abandons the `farewell` edit and rolls scenario16.py back to greet-only; the surviving branch then re-edits to add `shout`. First scenario where a structurally-discovered rewound branch (the S13 mechanism — abandoned tip #24093c68 is named by no last-prompt head) coexists with a surviving branch that records its OWN file change — the working-tree owner is the `shout` snapshot 2de1cd62 on the surviving chain, so findSurvivingHead keeps the final head a4ec5565 via its on-branch short-circuit (reconstruction_branch.ts:48-49; the S14 survivingBranchRecordsFileChange guard a second line of defense). So the surviving conversationDAG branch shows its `E edit` (not the `(no file changes)` marker S13/S14/S15 used). The lone `edited_text_file` echo (record 80, greet-only) is dropped by the S15 content-aware guard, so no user-edit turn appears and the fileDAG kind column stays width 5. NO engine change — locked by characterization/regression tests (GREEN on arrival, like S10/S11). 9 new tests (4 engine + 5 CLI); 206 green; S1–S15 byte-for-byte unchanged.
[x] S17 -> [x] multi-edit conversation-only rewind then post-rewind RE-EDIT (the conv-only twin of S16). The conv-only rewind (`Rewind: 2`, no `code`) abandons the `farewell` edit CONVERSATIONALLY but leaves it on disk, so the surviving branch re-edits the still-farewell file to add `shout` — surviving working tree is greet + farewell + shout (KEEPS farewell, inverting S16's greet + shout). The abandoned `farewell` edit is the same structurally-discovered rewound branch as S16 (tip #07038b43, rewind @ #4a69b697); the working-tree owner #60cee518 is on the surviving chain so findSurvivingHead keeps the final head #e53225b5 via its on-branch short-circuit. The surviving `shout` edit anchors on farewell, which is off the surviving branch's greet-only base, so the spec-39 resolveContextLine born-path materialises it. No `edited_text_file` echo exists (no user-edit turn). NO engine change — locked by characterization/regression tests (GREEN on arrival). 9 new tests (4 engine + 5 CLI); 215 green; S1–S16 byte-for-byte unchanged.
[x] S18 -> [x] linear external USER-EDIT with NO rewind, then a post-edit RE-EDIT on top (the inversion of S15). The user edits scenario18.py out-of-band (`# user was here`); because the content differs from what was written, the S15 content-aware guard KEEPS it as a recorded `user-edit` change on the SURVIVING lineage (S15 stranded its edit on a rewound branch; S18 has no rewind). Claude's `farewell` edit then anchors on the user-edited base, so the surviving tree is `# user was here` + greet + farewell. FIRST scenario whose fileDAG shows a `user-edit` kind on the surviving lineage and whose `--surviving` view KEEPS the user edit. NO engine change — locked by characterization tests (GREEN on arrival, like S10/S11/S16/S17). 9 new tests (4 engine + 5 CLI); 224 green; S1–S17 byte-for-byte unchanged.
[x] S19 -> [x] multi-edit USER-EDIT then CONVERSATION-ONLY rewind, then a post-rewind RE-EDIT (the conv-rewind twin of S18). B writes `add`; the user edits scenario19.py out-of-band (`# user tweak`, D); Claude edits to add `subtract` anchored on the user content (E); the user rewinds (`Rewind: 2`, no `code`) to the original prompt; Claude then edits to add `multiply` (F) on the surviving branch. D (user-edit) and E (subtract) end up on the REWOUND branch (tip #7087c57e, rewind @ #b1368b53), but because the rewind is conversation-only it does NOT revert the file on disk, so F's `multiply` edit was computed by Claude Code against an advanced disk state (`add` + 2 blanks + `subtract` + `# user tweak`) that the surviving branch's events (B + F only) do not contain. FIRST scenario where a surviving-branch edit was computed against a disk state produced by OFF-branch edits — the divergence dropped the two blank lines between `add` and `subtract` (`insertHunkAdditions` slice on a too-short base). FIRST scenario since S12 needing a PRODUCTION-CODE change: `seedStaleEditBases`/`backupSeedWriteFor` generalise spec 39's edit-base seeding to a MID-stream edit whose hunk references lines past its reconstructed base — the surviving F-edit's base is reseeded from the file-history v3 backup so the surviving tree ends at the real 11-line on-disk file. Safe by construction (`editBaseIsStale` is false for every aligned edit, so S1–S18 are byte-for-byte unchanged). 9 new tests (4 engine + 5 CLI); 233 green; S1–S18 byte-for-byte unchanged.
[x] S20 -> [x] multi-edit USER-EDIT then CODE rewind, then a post-rewind RE-EDIT (the code-rewind twin of S19). B writes `add`; the user edits scenario20.py out-of-band (`# user tweak` on line 3, D); Claude edits to add `subtract` anchored on the user content (E); the user rewinds (`Rewind: 2, code`) to the original prompt; Claude reads the file then edits to add `multiply` (G) on the surviving branch. D (user-edit) and E (subtract) end up on the REWOUND branch (tip #51227411, rewind @ #e76a23d3), but unlike S19's conversation-only rewind the CODE rewind REVERTS the file on disk — and Claude Code re-writing the checkpoint surfaces as a synthetic `edited_text_file` (user-edit F) on the SURVIVING branch (tip #cacc87c7). The S15 content-aware guard records F, advancing the surviving base to the full 3-line disk state (`add` + `# user tweak`); G's `multiply` edit was computed against exactly that base, so its hunk is ALIGNED → `editBaseIsStale` is false → the S19 `seedStaleEditBases` reseed stays INERT. FIRST scenario with a `user-edit` on BOTH branches (D = the real human edit on rewound; F = the code-rewind restore echo on surviving). NO engine change — locked by characterization/regression tests (GREEN on arrival, like S10/S11/S16/S17/S18), with test 3 guarding that the S19 reseed does not mis-fire (a 4th overwrite revision would go RED). 9 new tests (4 engine + 5 CLI); 242 green; S1–S19 byte-for-byte unchanged.
[x] S21 -> [x] MULTIPLE out-of-band USER edits interleaved with Claude edits on a single LINEAR no-rewind branch (the multi-edit extension of S18). B writes `Counter`+`__init__`; the user inserts `increment` (D); Claude adds `decrement` anchored on D's line (E); the user inserts `reset` (F); Claude adds `get_count` anchored on F's line (G); the user PREPENDS `# Counter class` (H). All three user edits change content, so the S15 content-aware guard records each as a `user-edit` revision; the surviving scenario21.py is six revisions whose kinds alternate write→userEdit→edit→userEdit→edit→userEdit, ending at the real 8-line on-disk file. FIRST scenario with THREE `user-edit` changes coexisting on one surviving lineage, the FIRST where each Claude edit is anchored on content a USER edit produced, and the FIRST with a PREPENDING user edit (H shifts `get_count` to line 8). Because there is no rewind every edit base is aligned, so the S19 `seedStaleEditBases` reseed stays INERT (no synthetic seed revision) — the regression lock proving dormancy across many interleaved edits. NO engine change — locked by characterization tests (GREEN on arrival, like S10/S11/S16/S17/S18/S20). 9 new tests (4 engine + 5 CLI); 251 green; S1–S20 byte-for-byte unchanged.
[x] S22 -> [x] TWO out-of-band USER edits across a CONVERSATION-only rewind, one on each branch. B writes `Stack`+`__init__`+`self.items = []`; on the abandoned branch the user inserts `push` (D) and Claude adds `pop` (E); a conversation-only Rewind:2 reverts the chat to the root prompt but leaves push+pop on disk; on the surviving branch the user inserts `peek` (F) and Claude adds `is_empty` (G). Because the rewind is conversation-only, the surviving user-edit F's `edited_text_file` snapshot ABSORBS the off-branch push+pop, so the surviving scenario22.py is three revisions (write→userEdit→edit) where rev1 jumps 3→6 lines, ending at the real 7-line on-disk file; the rewound branch reconstructs separately to the 5-line init+push+pop. FIRST scenario with a USER edit on EACH side of a conversation rewind, and the FIRST where a surviving user-edit materialises off-branch edits that survived the rewind. The same user-edit kind adds ONE line on the abandoned branch (push) but THREE on the surviving branch (push+pop+peek), driven by per-branch disk state. The S19 `seedStaleEditBases` reseed stays INERT (F is a user-edit, not a reseed candidate; G's base is aligned) — verified because the NO-reader reconstruction is byte-identical. NO engine change — locked by characterization tests (GREEN on arrival, like S10/S11/S16/S17/S18/S20/S21). 9 new tests (4 engine + 5 CLI); 260 green; S1–S21 byte-for-byte unchanged.
[x] S23 -> [x] TWO out-of-band USER edits across a CODE rewind, then a post-rewind RE-EDIT (the code-rewind twin of S22). B writes `Stack`+`__init__`+`self.items = []`; on the abandoned branch the user inserts `push` (D) and Claude adds `pop` (E); a `Rewind: 2, code` reverts BOTH the chat AND the disk to the root prompt, so `pop` is removed but the checkpoint keeps `push` (disk = init+push); the user then inserts `size` out-of-band, and Claude adds `is_empty` (G) anchored on the real init+size+push disk. CRITICAL: the user's `size` edit leaves NO file-change event in the JSONL — it surfaces only in the file-history backup `…@v5` and a later Read; the only surviving `edited_text_file` is the code-rewind restore echo (F = init+push). So G's `is_empty` edit was computed against a disk (init+size+push) that the surviving branch's on-branch events (B + F) do NOT reconstruct — the on-branch base is the SAME length (4 lines) but a WRONG line (`push` where `size` belongs). FIRST scenario since S19 requiring a PRODUCTION-CODE change: `editBaseIsStale` is generalised from a length-overflow check (`oldStart-1 > baseLength`, the S19 case) to a per-line CONTEXT-MATCH walk against the reconstructed base, so the existing `seedStaleEditBases` reseeds the `…@v5` backup before G and the surviving scenario23.py ends at the real 8-line on-disk file (size on line 4, a SINGLE push on line 5 — the unfixed engine dropped `size` and duplicated `push`). The rewound branch reconstructs separately to the 5-line init+push+pop. Safe by construction (every aligned edit matches its base line-for-line, so S1–S22 are byte-for-byte unchanged; the S19 length-overflow case is now a special case of the mismatch walk). 9 new tests (4 engine + 5 CLI); 269 green; S1–S22 byte-for-byte unchanged.
[x] M1 -> [x] cp-FORK: a `cp` forks a second file that then diverges. B writes `m1_base.py` (Config: debug/verbose=False); D adds `enable_debug`; E `cp m1_base.py m1_fork.py` (fork born = base@D, init+enable_debug, 7 lines); F adds `enable_verbose` to the FORK; G adds `disable_all` to the BASE. FIRST file-level fork (all of S7–S23 forked the conversation; m1 has no rewind — one surviving branch, tip #3740a519). Engine ALREADY correct (characterization LOCK, NO src change): copy born-content is the source AS OF THE cp moment (`seedOneCopy`/`lastRevisionAtOrBefore`), and per-path partitioning keeps base's `disable_all` out of the fork and the fork's `enable_verbose` out of the base. Reuses the S3 copy lineage; FIRST scenario to edit BOTH source and copy post-fork. 9 new tests (4 engine + 5 CLI); 278 green; S1–S23 byte-for-byte unchanged.
[x] M2 -> [x] mv-RENAME: a `mv` renames a file that is edited on BOTH sides of the rename. B writes `m2_old_name.py` (`process`); C writes the test; D adds `validate` to the old file; E `mv m2_old_name.py m2_new_name.py` (rename — old path ends, content carried = process+validate); F adds `finalize` to the NEW file. The renamed file reconstructs as ONE four-revision history (write→edit→rename→edit) ending at the 10-line process+validate+finalize file; `m2_old_name.py` collapses into `m2_new_name.py` (NOT a separate surviving file), so `reconstructAll` returns two histories and `--list-branches` lists only `m2_new_name.py, test_m2_old_name.py`. FIRST rename scenario edited on BOTH sides of the rename (the rename twin of m1's cp-fork; linear, no rewind — one surviving branch, tip #7260996e). Engine ALREADY correct (characterization LOCK, NO src change): `buildRenameChain`/`resolveFinalPath`/`eventBelongsToLineage` merge old+new into one lineage, `renameRevision` carries the pre-rename content forward (`lastLinesOf`/`carryAt`) so F composes on it, and `distinctFinalPaths` collapses the old path. Generalises the S2 move lineage from "edit only AFTER the move" to "edit on BOTH sides". 9 new tests (4 engine + 5 CLI); 287 green; S1–S23 + m1 byte-for-byte unchanged.
[x] M3 -> [x] bash-REDIRECT interleaved with an EDIT: one file m3_mixed.txt is written (`line one`), `>>`-appended (`line two`), edited (`line one`->`LINE ONE`), then `>>`-appended again (`line three`). The two `>>` redirects carry NO content in the JSONL — recovered from ~/.claude/file-history backups (@v3, @v5) via the BackupReader; the Edit's base (line one/line two) is content the append revision recovered from a backup. Reconstructs as ONE five-revision history [write, append, edit, edit, append] ending at the 29-byte `LINE ONE\nline two\nline three`; the Edit is the engine's standard removal+addition pair spliced onto the appended base, and — because the Edit's recorded base matches the reconstructed append revision — the S19/S23 stale-edit-base reseed stays INERT (exactly one write revision, five revisions not six). FIRST scenario to interleave an Edit between two bash redirects on one file, and the first to prove the S23 per-line editBaseIsStale walk does not false-positive on a backup-recovered append base. Linear (no rewind), one surviving branch tip #6076417b. Engine ALREADY correct (characterization LOCK, NO src change): parseRedirect/fillRedirectContent/findBackupAfter recover the append content, appendRevision carries+births it, applyEdit emits the removal+addition pair, and seedStaleEditBases/editBaseIsStale stay dormant. 9 new tests (4 engine + 5 CLI); 296 green; S1–S23 + m1 + m2 byte-for-byte unchanged.
[x] M4 -> [x] DELETE then RE-CREATE at the same path: TWO files. m4_lifecycle.py is written (v1 `def v1(): return 1`), edited (appends `v1_helper()`), DELETED via bash `rm`, then a NEW file is written at the same path (v2 `def v2(): return 2`); the sibling tests/test_m4_lifecycle.py is written (v1 test) then edited to v2. Reconstructs as TWO histories: m4_lifecycle.py is FOUR revisions [write, edit, delete, write] (delete = empty 0-line revision at rm time; the recreate is born FRESH — kind write not overwrite, every line genesis, carrying NONE of the pre-delete v1/v1_helper lineage), ending at the 23-byte `def v2():\n    return 2`; test_m4_lifecycle.py is THREE revisions [write, edit, edit] (the Edit is the engine's standard removal+addition pair) ending at the v2 test. FIRST scenario with a non-terminal delete, the FIRST write→delete→write recreate (exercising the fileIsPresent delete-branch / "locked decision 3"), and the first delete with a pre-delete edit + a sibling Edit-pair. Linear (no rewind), one surviving branch tip #3b6a446e. Engine ALREADY correct (characterization LOCK, NO src change): parseRmTarget/bashEventFrom emit the content-less DeleteEvent, deleteRevision yields the empty revision, writeRevision + fileIsPresent label the post-delete Write a fresh create, and applyEdit emits the removal+addition pair. Reader-free (no bash redirect). 9 new tests (4 engine + 5 CLI); 305 green; S1–S23 + m1 + m2 + m3 byte-for-byte unchanged.
[x] M5 -> [x] FULL INTERLEAVE: user + agent edits straddling a CODE REWIND on one file `m5_interleave.py` (plus a sibling `tests/test_m5_interleave.py`, write only). Ladder: Claude writes `base()`; the user out-of-band appends `user_add_1`; Claude adds `agent_add_1`; a `Rewind: 2, code` reverts chat+disk to the root prompt #b3aed408 (discards `agent_add_1`, keeps `user_add_1`); the user out-of-band appends `user_add_2`; Claude adds `agent_add_2`. Reconstructs as TWO branches: surviving (tip #93e7f94c) `m5_interleave.py` = base + user_add_1 + user_add_2 + agent_add_2 (5 lines) and the base test (1 revision); rewound (tip #fcd9c268, rewind @ #b3aed408) `m5_interleave.py` = base + user_add_1 + agent_add_1 (6 lines). The crux: `user_add_2` carries NO content in the JSONL — it lives ONLY in the file-history backup `17bbea89afb745a4@v5`, and the surviving `agent_add_2` Edit's recorded base includes that backup-only line, so its on-branch base reconstructs too SHORT (3 lines) and the S19 `seedStaleEditBases`/`editBaseIsStale` reseed FIRES, recovering `@v5` as a synthetic `overwrite` revision before the Edit (surviving source = 4 revisions [write, user-edit, overwrite, edit]); the FIRING complement of m3's dormant reseed and the first to recover a USER edit's disk from a backup. The rewound branch is reader-INDEPENDENT (`agent_add_1`'s base is aligned → reseed INERT). `user_add_1` is KEPT on the surviving lineage across the rewind (S18/S20/S22), surfacing on BOTH branches with DISTINCT changeIds (#5f68c3e1 rewound, #84166ae7 surviving). FIRST m-series scenario with a rewind (branched conversationDAG). Engine ALREADY correct (characterization LOCK, NO src change): cite `selectBranchRecords`, `buildRewoundBranchHistory`/`divergingIds`, `seedStaleEditBases`/`editBaseIsStale`/`backupSeedWriteFor`. 10 new tests (5 engine + 5 CLI); 315 green; S1–S23 + m1 + m2 + m3 + m4 byte-for-byte unchanged.
[x] M6 -> [x] cp-FORK + USER EDIT + CODE REWIND on the forked copy: `cp m6_source.py m6_derived.py` (m1 fork), a USER out-of-band edit inserts `# derived version` into the copy, Claude adds `transform()`, a `Rewind: 2, code` discards it, Claude adds `validate()`. TWO branches of m6_derived.py: surviving (tip #1d474d0b) = copy + `# derived version` + validate() (10 lines, 3 revisions [copy, user-edit, edit]) — already correct; rewound (tip #9b69e66c, rewind @ #a2903cef) = copy + `# derived version` + transform() (10 lines). THE BUG (FIRST wrong OUTPUT on a rewound branch, and the FIRST real engine fix since S19/S23): the original out-of-band user edit (8897e505) is on the abandoned lineage and `extractFileEvents` emits NO file event for it, so the rewound m6_derived lineage was just [copy, edit]; the S19/S23 stale-edit reseed correctly DETECTED the stale base but `backupSeedWriteFor` returned undefined because the only pre-edit backup (`b90d0fcb711472b4@v1`, 16:07:55.546) is 22ms AFTER the transform Edit's tool-use time (16:07:55.524) — the user edit and the edit consuming it share one turn — so `findBackupAtOrBefore` missed it and the transform Edit spliced onto the bare 6-line copy, DUPLICATING `return self.name` and dropping `# derived version`. THE FIX (2 src files, +21/−2): add `findBackupPointAfter` to `reconstruction_sidecar.ts` and an `includeAfter` flag on `backupSeedWriteFor` that falls back to the nearest strictly-later backup ONLY when no at-or-before backup exists; `reconstruction_branches.ts` `staleEditSeedFor` passes `true` (net-zero line — file at the 250 cap). Scoped so `seedEditBaseFromBackup` (spec-39 first-event-edit) keeps `includeAfter=false`, leaving every prior reseed byte-identical. The rewound endpoint is reader-DEPENDENT (unlike m5): without a BackupReader the duplicate persists. 12 new tests (2 sidecar + 5 engine + 5 CLI); 327 green; S1–S23 + m1–m5 byte-for-byte unchanged.
[x] M7 -> [x] CONVERSATION-ONLY REWIND, NO user edits. ONE source file m7_conv.py (+ sibling test). Write step1 → Edit +step2 → Edit +step3 → "Looking good" → Rewind:3 (conv-only; disk NOT restored) → Edit insert step2_alt before step3. TWO branches of m7_conv.py: rewound (tip #8037716c, rewind @ #a76d12e8) = step1→+step2→+step3 (3 revs [write, edit, edit], 10 lines), reader-INDEPENDENT (bases in JSONL); surviving (tip #b6d67431) = step1 → backup-seeded 10-line disk (overwrite #29a113119f194d6f@v4) → +step2_alt (3 revs [write, overwrite, edit], 14 lines), reader-DEPENDENT. FIRST pure conv rewind in the m-series and the FIRST time the S19/m5 stale-edit-base reseed fires because of OFF-BRANCH CLAUDE EDITS left on disk (not a user edit). Reader-ASYMMETRIC across branches (rewound independent, surviving dependent). Engine ALREADY correct (characterization LOCK, NO src change): findConversationBranches + findStructuralRewoundBranches enumerate the branches, editBaseIsStale/staleEditSeedFor/backupSeedWriteFor reseed the surviving base from backup, then the Edit replays. Does NOT use m6's includeAfter fallback (backup precedes the edit). 10 new tests (5 engine + 5 CLI); 337 green; S1–S23 + m1–m6 byte-for-byte unchanged.
[x] S24 -> [x] SCRIPT-DRIVEN function RENAME via Bash `python3` (the S-series resumes after the m-series detour). ONE source file `order_utils.py` (+ sibling `tests/test_order_utils.py` write + `rename_funcs.py` write). Ladder: Write `order_utils.py` (149 lines, terse names `calc_tot`/`fmt_money`/`chk_stock`/`mk_order`/`apply_disc`) → Edit +`validate_items` → Edit +`order_line` → Write `rename_funcs.py` then run it TWICE with `python3` via the Bash tool (it `open()`/`read()`/`re.sub`/`write()`s a whole-word rename `calc_tot→calculate_total`, `fmt_money→format_currency`, `chk_stock→check_stock`, `mk_order→build_order`, `apply_disc→apply_discount`) → Edit +`print_receipt` → Edit +`apply_loyalty`. The scenario FORBIDS Edit/Write for the rename — it happens ONLY through the script, so there is NO Edit/Write tool_use for the rename. `order_utils.py` reconstructs as SIX revisions [write, edit, edit, userEdit, edit, edit] ending at the 234-line / 6478-char ground truth. THE CRUX: the two `python3 rename_funcs.py` Bash runs are OPAQUE — NOT parsed as file ops (the m3 contrast, where `>>` redirects WERE parsed) and produce ZERO file events; what rescues S24 is a BEACON — immediately after the script runs the harness auto-snapshots the changed file as an `edited_text_file` ATTACHMENT (uuid `859347d2…`, message-UUID changeId), and the existing S15 machinery turns that snapshot into the rev3 `user-edit` carrying the renamed content. Post-rename Edits H/I replay cleanly on the renamed beacon base (recorded Edit base already shows the renamed disk), so the S19/S23 stale-edit reseed stays INERT (six revisions, not seven). This is the clean-room HAS-BEACON case (cite the sibling RevEng `*-script-execution-replay.md` spec/plan/tasks): a beacon exists with ZERO intervening edits, so the immediate post-script state is DIRECTLY OBSERVED — the engine adopts the beacon as the post-script revision and needs NO script-execution-replay / forward-validation. Reader-INDEPENDENT (content comes from the in-JSONL attachment snippet, not file-history backups — contrast m3/m5/m6/m7) — locked with a POISON reader. Linear (no rewind, one surviving branch, tip #fba814f7, `rewound.length === 0`). FIRST scenario where the `edited_text_file` disk-echo path (S15 family) is triggered by a SCRIPT (`python3`) rather than a human/IDE edit — the engine treats them identically, which is exactly the behavior locked. Engine ALREADY correct (characterization LOCK, NO src change): cite `userEditEventFrom`/`stripLineNumberPrefixes` (reconstruction_user_edit.ts), `userEditChangesContent`/`userEditRevision` (reconstruction_replay.ts), `collectEventsFromRecord`/`extractFileEvents` (reconstruction_extract.ts), and `applyEdit`/`editBaseIsStale`/`seedStaleEditBases` staying INERT. 10 new tests (5 engine + 5 CLI); 347 green; S1–S23 + m1–m7 byte-for-byte unchanged.
[x] S25 -> [x] MULTI-FILE script-driven RENAME via one Bash `python3 rename_geo.py` run — the multi-file extension of S24. Ladder: Write `geo_core.py` (terse `area`/`perim`/`vol`/`diag`/`scale`), `geo_report.py` (imports `geo_core`, defines `build_report`/`summary_line`), and `tests/test_geo_core.py` → Edit `geo_core` +`midpoint` → Edit `geo_report` +`summary_line` → Write `rename_geo.py` then run it with `python3` via the Bash tool (a whole-word rename ACROSS all three files: `area→rectangle_area`, `perim→rectangle_perimeter`, `vol→box_volume`, by `read()`/substitute/`write()`) → Edit `geo_report` +`totals` → Edit `geo_core` +`bounding_box`. The scenario FORBIDS Edit/Write for the rename — it happens ONLY through the script. THE CRUX: the single `python3` run is OPAQUE (NOT parsed, ZERO file events, the m3 contrast); what carries the rename is THREE `edited_text_file` BEACONs (one per file, uuids `dd04eabc…` geo_report / `755a78dd…` geo_core / `fcaacf80…` test, message-UUID changeIds) — the same S15 disk-echo machinery as S24, now firing three times from one Bash run. MIXED reader-dependence (the reason S25 exists): `geo_core.py` reconstructs as EIGHT revisions [write,edit,edit,edit,userEdit,edit,edit,edit] → 196 ln / 5263 ch, and `tests/test_geo_core.py` as TWO [write,userEdit] → 42 ln / 1145 ch, both reader-INDEPENDENT (complete beacons; later Edits splice cleanly; locked with a POISON reader). But `geo_report.py` is reader-DEPENDENT: its beacon is an INCOMPLETE 77-line snapshot (rev3), while the post-script `totals` Edit was computed against the true 95-line disk state, so the m5/m6 stale-edit-base machinery reseeds a synthetic `overwrite` rev4 keyed `a5675d5dd5201ac8@v4` (the 95-line backup) before replaying the Edit → SEVEN revisions [write,edit,edit,userEdit,overwrite,edit,edit] → 120 ln / 3810 ch. Without a backup the reseed can't fire (6 revs, truncated final — provably WRONG): the `@v4` backup is LOAD-BEARING. This REFINES the HAS-BEACON rule (`script-handling.txt`): "beacon alone suffices" is about the post-script revision itself; a LATER Edit's base alignment is the m6 concern, already solved by a backup-seed (NOT a script transform) — so S25 needs NO script-execution-replay / forward-validation feature. FIRST scenario where one Bash run yields multiple beacons AND the FIRST where a HAS-BEACON file still needs the backup reader. Linear (one surviving branch, tip #e3b43fcc, four files `geo_core.py`/`geo_report.py`/`test_geo_core.py`/`rename_geo.py`, `rewound.length === 0`). Engine ALREADY correct (characterization LOCK, NO src change): cite `userEditEventFrom`/`stripLineNumberPrefixes` (reconstruction_user_edit.ts), `userEditChangesContent`/`userEditRevision` (reconstruction_replay.ts), `collectEventsFromRecord`/`extractFileEvents` (reconstruction_extract.ts), and `seedEditBaseFromBackup`/`backupSeedWriteFor`/`findBackupPointAfter` (reconstruction_sidecar.ts) firing for geo_report / staying INERT for geo_core+test. 12 new tests (6 engine + 6 CLI); 359 green; S1–S24 + m1–m7 byte-for-byte unchanged.
[x] S26 -> [x] CSV-MAP multi-file script-driven RENAME — the data-driven variant of S25. ONE `python3 apply_renames.py` Bash run rewrites TWO tracked sources (`billing.py`, `tests/test_billing.py`), but the rename mapping is READ from a tracked `renames.csv` (header `old,new` + 4 pairs: `calc_tot→calculate_total`, `fmt_money→format_currency`, `chk_stock→check_stock`, `apply_disc→apply_discount`) rather than hardcoded in the script body. Ladder: Write `billing.py` (terse names) + `tests/test_billing.py` → Edit `billing` +`validate` → Write `renames.csv` → Write `apply_renames.py` then run it with `python3` via the Bash tool (it `open()`/`read()`/whole-word `re.sub`/`write()`s BOTH sources from the CSV map) → Edit `billing` +`print_invoice` (uses the renamed `calculate_total`/`format_currency`). The scenario FORBIDS Edit/Write for the rename. THE CRUX: the single `python3` run is OPAQUE (NOT parsed, ZERO file events, the m3 contrast); the rename is carried by TWO `edited_text_file` BEACONs (one per rewritten source, uuids `298a585d…` billing / `507c3e6b…` test_billing, message-UUID changeIds) via the same S15 disk-echo machinery. S26 is the STRUCTURAL INVERSION of S25's mixed-reader crux: `billing.py` ALSO has a post-script Edit (`print_invoice`, step 5), but its beacon is a COMPLETE 149-line snapshot, so that Edit splices cleanly (149→177) with NO backup-seed and NO `overwrite` revision — so ALL FOUR files are reader-INDEPENDENT (the m5/m6 `seedEditBaseFromBackup`/`backupSeedWriteFor` reseed stays DORMANT; provably ignored under a poison reader). This proves "post-script Edit ⇏ reader-dependent" — only an INCOMPLETE beacon (S25's `geo_report`) forces reader-dependence. `billing.py` reconstructs as FOUR revisions [write, edit, userEdit, edit] → 177 ln / 5813 ch (pure-insertion Edits, no removal/addition split); `tests/test_billing.py` as TWO [write, userEdit] → 60 ln / 1394 ch; `renames.csv` ONE [write] → 5 ln / 106 ch (the new wrinkle — the rename MAP is a tracked data file the script reads); `apply_renames.py` ONE [write] → 43 ln / 1125 ch. Test-file rename TRAP: the whole-word rename correctly LEAVES terse substrings inside test METHOD names (`test_calc_tot_empty`/`test_fmt_money_zero`), so the rename is locked via the renamed import line + call forms, NOT bare tokens. Linear (one surviving branch, tip #ae7838c8, four files, `rewound.length === 0`). Engine ALREADY correct (characterization LOCK, NO src change): cite `userEditEventFrom` (reconstruction_user_edit.ts), `userEditChangesContent`/`userEditRevision` (reconstruction_replay.ts), `collectEventsFromRecord`/`extractFileEvents` (reconstruction_extract.ts), and `seedEditBaseFromBackup`/`backupSeedWriteFor` (reconstruction_sidecar.ts) staying INERT (no `overwrite` reseed anywhere). Cite the HAS-BEACON rule (`script-handling.txt`). 12 new tests (6 engine + 6 CLI); 371 green; S1–S25 + m1–m7 byte-for-byte unchanged.
[x] S27 -> [x] SCRIPT RENAME with the rename script EDITED BEFORE it RUNS, plus a TERMINAL TRUNCATED BEACON — the FIRST `src/` change in the script-rename series (S24/S25/S26 were all LOCKs; S25 was *rescued* by a downstream Edit). ONE `python3 rename_inv.py` Bash run rewrites TWO tracked sources (`inventory.py`, `tests/test_inventory.py`) with three whole-word renames (`qty_chk→check_quantity`, `add_item→insert_item`, `rm_item→remove_item`); the scenario-naming twist is that `rename_inv.py` is WRITTEN then EDITED TWICE (the renames accrue across three script revisions) BEFORE the single run, and `inventory.py` gets one more Edit (`restock`) AFTER. Ladder: Write `inventory.py` (terse) + `tests/test_inventory.py` → Edit `inventory` +`low_stock` → Write `rename_inv.py` → Edit +`add_item` rename → Edit +`rm_item` rename → run `python3 rename_inv.py` (opaque, ZERO file events, the m3 contrast) → Edit `inventory` +`restock`. The rename surfaces via TWO `edited_text_file` beacons (uuids `a39c4731…` inventory / `51c639d6…` test_inventory). THE CRUX / the NEW failure mode: `tests/test_inventory.py`'s post-script beacon is TRUNCATED — a 50-line PREFIX of the true 73-line file — AND it is the file's LAST event (NO downstream Edit to reseed against, unlike S25's `geo_report`), so the engine adopts the truncated snippet verbatim and the file is short by 23 lines / 621 bytes (1594 vs 2215). THE FIX (REAL, reader-only): a new `completeTruncatedBeacon` event-list transform — when a file's LAST event is a `user-edit` beacon whose snippet is a byte-PREFIX of the file's LATEST file-history backup (`df7b79499e8a9377@v3`, 73 lines) AND the backup has strictly MORE lines, append a synthetic `write` from that backup so replay's terminal revision is the COMPLETE file (an `overwrite`). Backed by a new `latestBackupWriteFor` (reconstruction_sidecar.ts, picks the newest non-null backup — robust vs m6 ms-timing) and a new module `reconstruction_reseed.ts` (the stale-edit cluster MOVED out of `reconstruction_branches.ts` — already at the 250 cap — plus `beaconIsTruncated`/`completeTruncatedBeacon`). The LINE-COUNT (not byte-length) truncation test means a backup differing only by a trailing newline is NOT "truncated", so every COMPLETE terminal beacon (S25 `test_geo_core`, S15–S23 / m-series) passes through unchanged; the trigger fires ONLY when the last event is a `user-edit`, excluding `inventory.py`/`rename_inv.py` (end on Edit) and S25's `geo_report` (ends on `totals` Edit). Per-file reader-dependence is MIXED: `inventory.py` [write,edit,edit,edit,userEdit,edit] → 8442 ch and `rename_inv.py` [write,edit,edit,edit,edit] → 1449 ch are reader-INDEPENDENT (complete mid-stream beacon / no script touches the driver); `tests/test_inventory.py` [write,userEdit,overwrite] → 2215 ch is reader-DEPENDENT (without a reader it is the truncated 1594-ch 2-rev WRONG result; a poison reader is rejected by the `startsWith` guard — never injects garbage). Linear (one surviving branch, tip #7e94e313, three files, `rewound.length === 0`). 13 new tests (7 engine + 6 CLI); 384 green; S1–S26 + m1–m7 byte-for-byte unchanged.
[x] S28 -> [x] SCOPED script-driven RENAME (a CSV `isExported` column) plus ELIDED/WINDOWED BEACONs — the SECOND `src/` change in the script-rename series (after S27). ONE `python3 scoped_rename.py` Bash run rewrites tracked sources from a `scoped_renames.csv` whose rows are `old,new,isExported,file`: a `Y` row (`load_all→load_catalog`) renames ACROSS all files; an `N` row (`_norm→normalize_entry,catalog.py`) renames ONLY the named file. Ladder: Write `catalog.py` (terse `load_all`/`_norm`) → Edit `catalog` +`count_entries` → Write `catalog_view.py` (imports `catalog`, `render` calls `load_all`) + `tests/test_catalog.py` → Write `scoped_renames.csv` + `scoped_rename.py` then run it with `python3` (opaque, ZERO file events, the m3 contrast) → Edit `catalog_view` +`preview()` (calls the NEW `load_catalog`). The rename surfaces via THREE `edited_text_file` beacons (uuids `9d5b50af…` catalog / `888a7d75…` test_catalog / `c264b1e3…` catalog_view). THE CRUX / the NEW failure mode: TWO of the three beacons are ELIDED — the harness showed only a WINDOW of the file (bare `...` separators and/or starting past line 1), so `extractFileEvents`+`stripLineNumberPrefixes` adopt the windowed snippet verbatim (the line NUMBERS that reveal the gaps are discarded). `tests/test_catalog.py`'s beacon is TERMINAL MID-ELIDED (lines 1–9, `...`, 17–33, `...`, 39–102) — NOT a byte-prefix, so S27's `completeTruncatedBeacon` cannot fire — leaving the file 92 ln / 2393 ch (true: 102 / 2665). THE FIX (REAL, reader-only): a new `completeElidedBeacons` event-list transform — for EACH `user-edit` beacon whose RAW `cat -n` snippet is elided (starts past line 1, has a line-number gap, or carries a literal `...`), find the file-history backup version whose numbered content reproduces EVERY visible beacon line (forward-validation) and splice a synthetic `write` of it immediately AFTER the beacon. Backed by a new `beaconSnippetFor`/`BeaconSnippet` parser (reconstruction_user_edit.ts — re-reads the RAW numbered snippet that `stripLineNumberPrefixes` discarded) and a new `backupWritesFor` (reconstruction_sidecar.ts — enumerates ALL backup versions; `latestBackupWriteFor` now reuses it). VERSION-SELECTION BY CONTENT, not recency: `catalog_view.py`'s correct base is `a8b61336832f339e@v3` (post-script, pre-`preview`), NOT the latest `@v4` (post-`preview`, whose lines 11–41 shifted and FAIL `backupMatchesBeacon`) — the FIRST time a non-latest backup is selected. Wired BEFORE `seedStaleEditBases` and `completeTruncatedBeacon` (disjoint from S27: a contiguous-from-1 prefix is NOT elided). Per-file reader-dependence is MIXED: `catalog.py` [write,edit,edit,edit,userEdit] → 6749 ch (COMPLETE beacon), `scoped_rename.py` [write] → 1898 ch, `scoped_renames.csv` [write] → 83 ch are reader-INDEPENDENT; `tests/test_catalog.py` [write,userEdit,overwrite] → 2665 ch and `catalog_view.py` [write,userEdit,overwrite,edit,edit,edit] → 2067 ch are reader-DEPENDENT. DEVIATION FROM PLAN (verified live): only `tests/test_catalog.py` (the TERMINAL elided beacon) genuinely needs the new code — `catalog_view.py`'s NON-terminal beacon was ALREADY completed to 2067 by the pre-existing `seedStaleEditBases` (s19/s23), since the window renumbers its `preview` Edit anchors so `editBaseIsStale` returns TRUE; `completeElidedBeacons` is its explicit primary handler with `seedStaleEditBases` as an inert fallback (so neutralizing `beaconIsElided` turns ONLY the `test_catalog` crux red, not both). Linear (one surviving branch, tip #2114511a, six surviving incl. the stray `/tmp/cat.txt` cat-redirect, `rewound.length === 0`). 14 new tests (8 engine + 6 CLI); 398 green; S1–S27 + m1–m7 byte-for-byte unchanged.
[x] S29 -> [x] REPO-WALK script-driven RENAME with a SKIPPED `vendor/` directory — a CHARACTERIZATION/REGRESSION LOCK (NO `src/` change; the FIRST script-rename lock since S26). ONE `python3 walk_rename.py` Bash run `os.walk`s the tree, collects every `.py` file SKIPPING any dir named `vendor`, and whole-word renames `helper→compute_value` in each — the file list is DISCOVERED by walking, not hardcoded. Ladder: Write `pkg/a.py` (data-loading, defines `helper`) + `pkg/b.py` (reporting, defines `helper`) + `pkg/vendor/c.py` (vendored, defines `helper` — the CONTROL) + `main.py` + `tests/test_pkg.py` (+ `pkg/__init__.py`, `pkg/vendor/__init__.py`) → Edit `pkg/a.py` +`preprocess` (PRE-rename) → Write `walk_rename.py` then run it with `python3` (opaque, ZERO file events, the m3 contrast) → Edit `pkg/b.py` +`pipeline` (POST-rename, calls the NEW `compute_value`). The single run emits SIX `edited_text_file` beacons (uuids `1527f907…` pkg/__init__ / `e30c1bc6…` a.py / `4c52c762…` b.py / `b27e66ab…` test_pkg / `1e9f8dd1…` main / `d16b6b3b…` walk_rename) in EVERY shape the engine already handles, all in one run: ONE COMPLETE (`pkg/__init__.py` 1–13, adopted verbatim), FOUR TRUNCATED (`tests/test_pkg.py` 1–34/51, `main.py` 1–40/71, `pkg/b.py` 1–93/101, `walk_rename.py` 1–9/51 — S27 `completeTruncatedBeacon`), ONE ELIDED (`pkg/a.py` 1–99 + `...` of 128 — S28 `completeElidedBeacons`). THE HEADLINE LOCK: the two `pkg/vendor/*` files get NO beacon (the walk skipped them) so the engine keeps their original `write` — they must still read `helper`, never `compute_value`, reader-INDEPENDENT + poison-stable (the control). S29 is thus the COMPOSITION test: S27 + S28 + vendor-skip all fire under one recursive-walk run, byte-perfect, with no new engine code. VERSION-SELECTION BY CONTENT, not recency (the strongest crux): `pkg/a.py`→`38dbed748662c3cb@v4` (128L, compute_value) chosen over the SAME-128-line `@v3` (helper, pre-rename); `pkg/b.py`→`e5663c2564dcb2d1@v3` (101L) chosen over the same-101-line `@v2`, then the `pipeline` Edit replays on top. SELF-MODIFYING script: `walk_rename.py` is itself walked, so the rename rewrites its own string literals (`'helper'`→`'compute_value'`) — its final shows `compute_value` and no bare `helper`. Per-file: `pkg/__init__.py` [write,userEdit]→399 ch + the two vendor controls (`pkg/vendor/c.py` [write]→2031 ch, `pkg/vendor/__init__.py` [write]→289 ch) are reader-INDEPENDENT; the four truncated/elided files + `pkg/b.py` are reader-DEPENDENT (`pkg/a.py` [write,edit,userEdit,overwrite]→3460, `pkg/b.py` [write,userEdit,overwrite,edit]→3330, `main.py` [write,edit,edit,edit,userEdit,overwrite]→1788, `tests/test_pkg.py` same ladder→1601, `walk_rename.py` [write,userEdit,overwrite]→1530). `pkg/b.py` LEAKS poison under a degenerate reader via the pre-existing `seedStaleEditBases` (s19/s23) path for the `pipeline` Edit's stale base — established prior-scenario behaviour, EXCLUDED from poison asserts. GROUND-TRUTH GAP: the executed-scenario folder captured only `main.py`/`walk_rename.py`/`tests/test_pkg.py`; the `pkg/` subtree was NOT rendered, so the `pkg/*` cruxes are locked via inline file-history backup blobs (engine test) and the real reader (CLI test). Mutation probes: neutralize `completeTruncatedBeacon` breaks ONLY `main.py`/`test_pkg.py`/`walk_rename.py` (NOT `pkg/b.py` — its truncated beacon is independently repaired by `seedStaleEditBases` via the later Edit); neutralize `completeElidedBeacons` breaks ONLY `pkg/a.py`. Linear (one surviving branch, tip #e6bfddf3, eight files, `rewound.length === 0`). 14 new tests (8 engine + 6 CLI); 412 green; S1–S28 + m1–m7 byte-for-byte unchanged.
[x] S30 -> [x] COUNT-MISMATCH script-driven RENAME — a CHARACTERIZATION/REGRESSION LOCK (NO `src/` change), the FIRST script-rename where the script's own logic CONDITIONALLY REFUSES part of its work. ONE `python3 safe_rename.py` Bash run reads `count_renames.csv` (header `old,new,count` + two rows) and, for each row, counts whole-word occurrences of `old` in `pricing.py`; IFF the count equals the row's `count` it applies the rename to BOTH `pricing.py` and `tests/test_pricing.py`, ELSE it prints `MISMATCH` and leaves that name unchanged everywhere (no partial write). The CSV is the off-by-one proof: `round_price,round_to_cents,8` (actual count 8 == 8 → APPLIED) and `base_price,unit_price,10` (actual count 11 != 10 → REFUSED). Ladder: Write `pricing.py` (terse `base_price`/`net_price`/`bulk_price`/`round_price`/`tax_price`, `base_price` referenced several times) + `tests/test_pricing.py` → Edit `pricing` +`quote()` (calls `base_price` then `round_price`; rendered as two intermediate edit revisions) → Write `count_renames.csv` → Write `safe_rename.py` then run it with `python3` (opaque, ZERO file events, the m3 contrast) → confirm `MISMATCH` for the `base_price` row → Edit `pricing` +`receipt()` (calls the NEW `round_to_cents`). The applied rename surfaces via TWO `edited_text_file` beacons (uuids `9a1c303d…` pricing / `388074da…` test_pricing). THE HEADLINE LOCK: the engine adopts the post-script beacons and NEVER re-derives the rename, so the refusal is captured FOR FREE — the beacons already show `round_to_cents` applied and `base_price` retained, and nothing in the pipeline can fabricate `unit_price` (it exists in no beacon, edit, or backup — only as a string in `count_renames.csv`). S30 is thus the S28 elided path + a refusal that costs nothing: `pricing.py` [write,edit,edit,userEdit,edit] → 5863 ch carries a COMPLETE 155-line beacon then the downstream `receipt` Edit, making it reader-INDEPENDENT (real==without==poison; Probe C proved `seedStaleEditBases` INERT here); `tests/test_pricing.py` [write,userEdit,overwrite] → 1035 ch has an ELIDED 39-line beacon (lines 1–40, `...` eliding 19–20) completed by S28 `completeElidedBeacons`, reader-DEPENDENT. VERSION-SELECTION BY CONTENT, not recency: the chosen overwrite base is `b1770edab554937c@v3` (40L, post-rename `round_to_cents`) over the SAME-40-line `@v2` (pre-rename `round_price`) — both keep `base_price`×5, so no version contains `unit_price`. `count_renames.csv` [write] → 67 ch and `safe_rename.py` [write] → 2252 ch are reader-INDEPENDENT (no beacon — the script never renames them). NO poison leak anywhere — a fully clean reader/poison matrix (unlike S29's `pkg/b.py`). Mutation probes (live): neutralize `completeElidedBeacons` → ONLY `tests/test_pricing.py` diverges (1035→977, `...` survives — the sole crux); `completeTruncatedBeacon` and `seedStaleEditBases` both INERT (nothing diverges). Linear (one surviving branch, tip #e61d0ae0, four files, `rewound.length === 0`). 12 new tests (6 engine + 6 CLI); 424 green; S1–S29 + m1–m7 byte-for-byte unchanged.
[x] S31 -> [x] MANY-ROW script-driven RENAME — a CHARACTERIZATION/REGRESSION LOCK (NO `src/` change), the many-row sibling of S25/S26/S29 that locks SCALE: twelve whole-word renames in a single run with no per-row degradation. ONE `python3 bulk_rename.py` Bash run reads a TWELVE-row `many_renames.csv` (header `old,new` + 12 rows: `cap,capitalize` `low,lowercase` `up,uppercase` `rev,reverse` `trim,trim_whitespace` `pad,pad_right` `cnt,count` `idx,index_of` `rep,replace` `splt,split` `joi,join` `slug,slugify`) and applies every `\bold\b`→`new` rename across `textutil.py` and `tests/test_textutil.py`. Ladder: Write `textutil.py` (193 L, terse `cap`/`low`/`up`/`rev`/`trim`/`pad`/`cnt`/`idx`/`rep`/`splt`/`joi`/`slug` defs) + `tests/test_textutil.py` (imports `cap, rev, slug`) → Edit `textutil` +`normalize()` (PRE-script; its body references the OLD `trim`/`low`) → Write `many_renames.csv` + `bulk_rename.py` then run it with `python3` (opaque, ZERO file events, the m3 contrast) → Edit `textutil` +`headline()` (POST-script; references the NEW `capitalize`/`slugify`). The single run emits TWO COMPLETE `edited_text_file` beacons (uuids `98ce2cbf…` textutil 205 L full file / `590f882d…` test_textutil 71 L full file). THE HEADLINE LOCK: both beacons are COMPLETE, so the engine adopts the fully-renamed state FOR FREE — no rescue stage fires (`completeTruncatedBeacon` S27 / `completeElidedBeacons` S28 / `seedStaleEditBases` S19 all INERT), reader-INDEPENDENT, clean poison matrix. THE EDIT-ORDERING CRUX: `normalize` (added BEFORE the run) had its body RENAMED by the script (final `normalize` docstring references `:func:\`trim_whitespace\``/`:func:\`lowercase\``, not old `trim`/`low`); `headline` (added AFTER the run) replays on TOP of the renamed beacon (rev 3) and references the NEW `capitalize`/`slugify`. WHOLE-WORD vs SUBSTRING hazard: 11 of 12 old names have ZERO whole-word matches in final `textutil.py`, but `\bslug\b` legitimately appears TWICE (English prose in `headline`'s docstring, written post-rename — NOT a missed rename); test fn names keep terse substrings (`test_cap_basic`/`test_rev_*`/`test_slug_*`, `_`-bounded) — every absence assertion uses `\bname\b`, never bare `includes()`. Per-file: `textutil.py` [write,edit,userEdit,edit] → 218 L, `tests/test_textutil.py` [write,userEdit] → 71 L, `many_renames.csv` [write] → 13 L, `bulk_rename.py` [write] → 54 L; ALL reader-INDEPENDENT. `extractFileEvents`: writes=4, edits=2 (`normalize`+`headline`, both on textutil.py), userEdits=2 (`98ce2cbf,590f882d`), overwrites=0 — NOTE 2 edits, not 3 (re-verified live; the only edits are textutil.py's two bracketing Claude edits). Linear (one surviving branch, tip #1349c502, four files, `rewound.length === 0`). 12 new tests (6 engine + 6 CLI); 436 green; S1–S30 + m1–m7 byte-for-byte unchanged.
[x] S32 -> [x] MCP-EXEC script-driven RENAME — the FIRST script-rename whose rename script runs through the **context-mode MCP sandbox** (`mcp__plugin_context-mode_context-mode__ctx_execute`, shell `python3 rename_config.py`) instead of the Bash tool, and the FIRST `src/` change in the s25–s32 series — a ONE-LINE PARSER fix (the FIRST change to `src/parse/loadTranscript.ts` rather than the reconstruction/replay layer). THE GAP: the 19 MCP-invoking assistant records each carry two novel top-level keys — `attributionMcpServer` (`plugin:context-mode:context-mode`) and `attributionMcpTool` (`ctx_execute`) — absent from `ALLOWED_TOP_LEVEL_KEYS[assistant]`; at HEAD `assertOnlyKnownTopLevelKeys` throws `UnmodeledFieldError` at PARSE time, gating EVERY file (nothing reconstructs at all). THE FIX: add the two keys to the assistant entry of `ALLOWED_TOP_LEVEL_KEYS` (assistant-only attribution fields — NOT added to `ENVELOPE_KEYS`, since only MCP-calling assistant records carry them; `assertOnlyKnownTopLevelKeys` is purely permissive and no downstream code reads them). `entrypoint`, the other novel-looking key here, is ALREADY in `ENVELOPE_KEYS` → no change. After the fix the whole scenario reconstructs byte-perfectly with NO further change. The rename: whole-word `get_val`→`get_value`, `set_val`→`set_value`, `del_val`→`delete_value` across `config_utils.py` and `tests/test_config_utils.py` (`has_val`/`merge_val` kept). Ladder: Write `config_utils.py` + `tests/test_config_utils.py` → Edits adding `get_or_default` (PRE-script; body calls the OLD `get_val`) → Write `rename_config.py` then run it via the MCP sandbox (opaque, ZERO file events — the Bash-run contrast of s25–s31) → Edits adding `apply_overrides` (POST-script; calls the NEW `get_value`/`set_value`). THE HEADLINE LOCK: the single MCP run emits TWO COMPLETE `edited_text_file` beacons (uuids `d9cbc214…` config / `c7922fde…` test) the harness injects right after it — the SAME HAS-BEACON mechanism as s24/s31 — so both renamed files are reader-INDEPENDENT and the engine adopts the fully-renamed state FOR FREE; every rescue stage INERT (`completeTruncatedBeacon` S27 / `completeElidedBeacons` S28 / `seedStaleEditBases` S19), clean poison matrix. `plans/script-handling.txt` already names the MCP sandbox as a HAS-BEACON-governed invisible-mutation source. THE EDIT-ORDERING CRUX: `get_or_default` (added BEFORE the run) had its body RENAMED by the script (final references `get_value`, no whole-word `\bget_val\b`); `apply_overrides` (added AFTER the run) replays on TOP of the renamed beacon and references the NEW `get_value`/`set_value`. WHOLE-WORD vs SUBSTRING hazard: the test file keeps the `_`-bounded def names `test_get_val_flat_key`/`test_set_val_overwrites_existing` (NOT whole-word matches — not missed renames); every absence assertion uses `\bname\b`, never bare `includes()`. Per-file: `config_utils.py` 11 revisions → 260 L, `tests/test_config_utils.py` 2 revisions → 87 L, `rename_config.py` 1 revision → 62 L; both renamed files reader-INDEPENDENT. `extractFileEvents`: writes=3, edits=6 (all on config_utils.py), userEdits=2 (`c7922fde,d9cbc214`), overwrites=0. Linear (one surviving branch, tip #1be0133f, three files, `rewound.length === 0`). MUTATION PROOF: reverting the one-line `loadTranscript.ts` fix sends 7 s32 tests RED with `UnmodeledFieldError` — the parser-gate test + all 6 CLI tests, which load through the GATED `loadTranscript` (`runCli`); re-applying restores green (necessary + sufficient). NOTE: the 8 engine tests build via the UNGATED `loadRecords` (`parseRecord`, no field gate — the established s31 pattern), so they lock the reconstruction OUTPUT byte-for-byte but stay green across the revert (they don't exercise the gate); the parser fix's necessity is proven by the gated CLI + parser-gate tests. 15 new tests (1 parser-gate + 8 engine + 6 CLI); 451 green; S1–S31 + m1–m7 byte-for-byte unchanged.
[x] S33 -> [x] CSV-USER-EDIT script-driven RENAME — a CHARACTERIZATION/REGRESSION LOCK (NO `src/` change), the sibling of S25/S29/S31/S32 whose NOVEL twist is that the rename MAPPING file itself is USER-EDITED before the script consumes it. ONE `python3 apply_renames.py` Bash run reads `renames.csv` and applies each `\bold\b`→`new` whole-word rename across `billing.py` and `tests/test_billing.py`. THE NOVEL ELEMENT: `renames.csv` is WRITTEN with a header + 3 rows (`calc_tot,calculate_total` / `fmt_money,format_currency` / `apply_disc,apply_discount`) then USER-EDITED to APPEND a 4th row `chk_stock,check_stock` BEFORE the script runs — so `renames.csv` reconstructs to a 2-revision ladder (rev 0 = 4 lines WITHOUT the 4th row; rev 1 = 5 lines WITH it). THE CSV USER-EDIT IS LOAD-BEARING: the post-script `reorder` Edit on `billing.py` calls `check_stock`, a name that exists ONLY because that manual 4th row flowed through the script. Ladder: Write `billing.py` (terse `calc_tot`/`fmt_money`/`chk_stock`/`apply_disc` + helpers) + `tests/test_billing.py` → Edit `billing` +`validate()` (PRE-script; references no terse name, so the rename leaves it untouched) → Write `renames.csv` (3 rows) → User-edit `renames.csv` (append `chk_stock,check_stock` — a GENUINE manual `edited_text_file` edit, NOT a script beacon) → Write `apply_renames.py` then run it with `python3` (opaque, ZERO file events, the m3 contrast) → Edit `billing` +`reorder()` (POST-script; calls the NEW `check_stock` + `calculate_total`). The single run emits TWO COMPLETE `edited_text_file` beacons (uuids `4480f645…` billing / `c7415420…` test). THE HEADLINE LOCK: both beacons are COMPLETE, so the engine adopts the fully-renamed state FOR FREE — no rescue stage fires (`completeTruncatedBeacon` S27 / `completeElidedBeacons` S28 / `seedStaleEditBases` S19 all INERT), every file reader-INDEPENDENT, clean poison matrix. THREE user-edits, MIXED provenance: `4480f645` (billing) + `c7415420` (test) are the script-run beacons, `b8591d24` (renames.csv) is the manual CSV edit — the engine treats all three UNIFORMLY, which is why no engine change is needed. NO kept-name hazard (unlike S31/S32): `tests/test_billing.py` method names (`test_empty_is_zero`/`test_single_item`/…) embed NO terse substring — still every absence assertion uses `\bold\b`, never bare `includes()` (the renamed import + call sites are the real surface). Prose hazard: the NEW names appear ~16× in `billing.py` docstrings (e.g. `reorder`'s `:func:\`check_stock\``) — absence assertions target OLD whole-word names ONLY, never new names. Per-file: `billing.py` [write,edit,userEdit,edit] → 4 revisions / 187 L (146→164→164→187), `tests/test_billing.py` [write,userEdit] → 2 revisions / 51 L, `renames.csv` [write,userEdit] → 2 revisions / 5 L (4→5), `apply_renames.py` [write] → 1 revision / 51 L; ALL reader-INDEPENDENT. `extractFileEvents`: writes=4, edits=2 (`validate`+`reorder`, both on billing.py), userEdits=3 (`4480f645,b8591d24,c7415420`), overwrites=0. Linear (one surviving branch, tip #c3d6846d, four files, `rewound.length === 0`). 12 new tests (6 engine + 6 CLI); 463 green; S1–S32 + m1–m7 byte-for-byte unchanged.
[x] S34 -> [x] DRIVER-BACK-AND-FORTH script-driven RENAME — a REAL ENGINE FIX (the first since m6/S28) plus a MANDATORY 250-line MODULE SPLIT. ONE `python3 apply_renames.py` Bash run reads `renames.csv` and applies four `\bold\b`→`new` whole-word renames (`add_entry→record_entry` / `rm_entry→remove_entry` / `tot_debits→total_debits` / `tot_credits→total_credits`) across `ledger.py` and `tests/test_ledger.py`. THE "BACK AND FORTH": `renames.csv` is WRITTEN with header + 2 rows → manual user-edit appends a 3rd row (`tot_debits,total_debits`) → the driver `apply_renames.py` is WRITTEN → manual user-edit appends a 4th row (`tot_credits,total_credits`) — the driver write sits BETWEEN two manual CSV edits, and the last two renames exist ONLY because of those interleaved manual rows. Ladder: Write `ledger.py` (terse `add_entry`/`rm_entry`/`tot_debits`/`tot_credits`/`bal` + helpers, 156 L) + `tests/test_ledger.py` (57 L) → User-edit `ledger` prepend `# ledger module — managed by finance` (157 L beacon) → Edit `ledger` +`audit()` (PRE-script; its body uses the OLD `tot_debits`/`tot_credits`, 173 L) → Write `renames.csv` (2 rows) → User-edit `renames.csv` append `tot_debits,total_debits` (4-line beacon) → Write `apply_renames.py` (driver; whole-word `re.sub(r"\b" + re.escape(old) + r"\b", new, text)` from the CSV) → User-edit `renames.csv` append `tot_credits,total_credits` (5 lines, NO BEACON — recovered from a file-history backup via the EXISTING S27 `completeTruncatedBeacon`) → run `python3 apply_renames.py` (opaque, ZERO file events, the m3 contrast; renames BOTH sources, 173 L) → User-edit `ledger` append `# names normalized via rename script` (174 L, **NO BEACON** — the sole engine gap) → Edit `ledger` +`report()` (POST-script; calls the NEW `record_entry`/`total_debits`, 187 L). THE NEW FAILURE MODE: the step-9 trailing append left no `edited_text_file` beacon and no tool_use, and it lands OUTSIDE the `report` edit's hunk window — whose context (`if __name__ == "__main__":`) still matches the 173-line script-rename beacon base perfectly — so the existing hunk-context staleness test (`editBaseIsStale`) returns FALSE, no reseed fires, and the trailing line is silently dropped (`ledger.py` reconstructs to 186; ground truth 187). THE FIX (REAL, reader-only, in `src/reconstruction_reseed.ts`): a SECOND staleness trigger — `outOfWindowEditSeed` detects an Edit whose hunk context matches the reconstructed base but whose real pre-edit disk carried an UNCAPTURED change outside the window, by CONTENT: a file-history backup (`d5ade1bd80e08f91@v4`, 174 L, taken AFTER the beacon at `19:51:20` and AT/BEFORE the `report` edit at `19:51:37`) that is STRICTLY NEWER than the file's last captured event, whose content DIFFERS from the reconstructed base, AND onto which the edit's first hunk still splices cleanly (forward-validation — a poison/wrong backup is rejected, never fabricated). `editBaseIsStale` is refactored to share a new `firstHunkMatchesBase` with this path; the two triggers are DISJOINT (s19/s23/m6 fires only when the hunk context is STALE, s34 only when it is CLEAN). Gated inside `seedStaleEditBases` (reader present) → reader-free reconstruction byte-for-byte untouched. MANDATORY MODULE SPLIT (`split, never condense`): the fix pushes `reconstruction_reseed.ts` past the 250-line cap, so the beacon-completion family (`completeTruncatedBeacon` S27 / `completeElidedBeacons` S28 + private helpers `beaconIsTruncated`/`beaconIsElided`/`backupMatchesBeacon`/`elidedBeaconSeed`) MOVED to a new `src/reconstruction_beacons.ts`; `reconstruction_branches.ts` imports each module directly (NO re-export shim — no-forwarding-layers). Both modules end at 145 lines. S34 is reader-DEPENDENT (no-reader: `ledger.py`=186, `renames.csv`=4 — the RED proof; real reader after fix: 187/5; a poison reader is rejected by the forward-validation guards, falling back to 186/4 — never fabricated). EDIT-ORDERING CRUX: `report` (added AFTER the run) replays on the renamed beacon and calls the NEW `record_entry`/`total_debits`; `audit` (added BEFORE the run) had its body RENAMED in place by the script (now references `total_debits`/`total_credits`, no whole-word old names); the step-9 trailing comment survives beneath both. WHOLE-WORD hazard: the new names recur in `ledger.py` docstrings/prose — absence assertions target OLD whole-word names ONLY (`\badd_entry\b`/`\brm_entry\b`/`\btot_debits\b`/`\btot_credits\b`), never new names. The renamed test import surface is `from ledger import record_entry, bal, remove_entry`. `extractFileEvents`: writes=4, edits=2 (`audit`+`report`, both on ledger.py), userEdits=4 (`0ee68aad,2a0d75ba,720ee20c,ba8ee917` — the step-7 and step-9 appends left NO beacon, so they are NOT in the multiset; recovered from backups at replay), overwrites=0. Per-file (real reader): `ledger.py` [write,userEdit,edit,userEdit,edit] → 6 revisions / 187 L (156→157→173→173→174→187, the 174 = the synthetic out-of-window reseed), `tests/test_ledger.py` [write,userEdit] → 2 / 57 L, `renames.csv` [write,userEdit] → 3 / 5 L (3→4→5, the 5th via S27), `apply_renames.py` [write] → 1 / 54 L. The synthetic reseed Write's changeId is the backup blob name, kept out of the DAGs (spec 40) → `ledger.py`'s fileDAG stays 5 nodes (B,D,E,I,K), surfacing only as a `--verbose` revision. Linear (one surviving branch, tip #97e510eb, four files, `rewound.length === 0`). FAILED `report` edit (`toolu_01Q1y1…`, "file modified since read", no `structuredPatch`) is the on-disk-modified marker — it produces no event/node; only the successful retry `#01KcAiCq` is in the lineage. 12 new tests (6 engine + 6 CLI); 475 green; S1–S33 + m1–m7 byte-for-byte unchanged.
[x] S35 -> [x] SCRIPT-USER-EDIT script-driven RENAME — a CHARACTERIZATION/REGRESSION LOCK (NO `src/` change), the COMPLEMENT of S33: the SAME "a file is user-edited before the script run" shape, but where S33's beacons were all COMPLETE (every rescue stage INERT), S35's script-phase beacons are INCOMPLETE so the EXISTING rescue stages FIRE. It composes already-shipped machinery (S27 `completeTruncatedBeacon` + S28 `completeElidedBeacons` + S15 native user-edit) with no new code. ONE `python3 rename_inv.py` Bash run applies three `\bold\b`→`new` whole-word renames (`qty_chk→check_quantity` / `add_item→insert_item` / `rm_item→remove_item`) across `inventory.py` and `tests/test_inventory.py`. THE NOVEL ELEMENT: the file user-edited before the run is the RENAME SCRIPT ITSELF — `rename_inv.py` is WRITTEN with ONE rename tuple (`("qty_chk","check_quantity")`), then USER-EDITED TWICE (steps 4 & 5 insert the `add_item` then `rm_item` tuples), and those two edits COALESCE into a SINGLE ELIDED `edited_text_file` beacon (`#c67cfd9c`, a 17-line head+tail-cut fragment whose first line is `boundaries) so that substrings inside longer identifiers are left alone.`) — the intermediate 2-tuple state is NOT independently observable. Ladder: Write `inventory.py` (terse `qty_chk`/`add_item`/`rm_item`/`find_item`/`tot_value` + helpers, 172 L) + `tests/test_inventory.py` (79 L) → Edit `inventory` +`low_stock()` (PRE-script; references no terse name, so the rename leaves it untouched) → Write `rename_inv.py` (1 tuple, 43 L) → User-edit `rename_inv.py` +`add_item` tuple → User-edit `rename_inv.py` +`rm_item` tuple (these two COALESCE into the single elided beacon) → run `python3 rename_inv.py` (opaque, ZERO file events, the m3 contrast) → Edit `inventory` +`restock()` (POST-script; calls the NEW `check_quantity`/`insert_item`). The single run emits TWO `edited_text_file` beacons (uuids `dbc2e4c1…` inventory = COMPLETE / `f3e90535…` test = TRUNCATED). THE HEADLINE LOCK: MIXED beacon completeness in one run, and the engine completes both incomplete beacons from file-history backups FOR FREE. `rename_inv.py`'s coalesced ELIDED beacon (17 L) is completed by S28 `completeElidedBeacons` from backup `41364cab6ad88cbb@v2` → 45 L (all three tuples + the whole-word string-concat substitution literal `re.sub(r"\b" + re.escape(old) + r"\b", new, text)`); `tests/test_inventory.py`'s TRUNCATED beacon (a 51-line prefix) is completed by S27 `completeTruncatedBeacon` from backup `5ea404c2628560f6@v3` → 79 L; `inventory.py`'s COMPLETE beacon needs no rescue (reader-INDEPENDENT, 244 L). S35 is reader-DEPENDENT (MIXED, like S25): with NO reader the two incomplete beacons fall back to their raw fragments (`test`=51 L/2-revs, `rename`=17 L/2-revs) — the reader-dependence proof — while `inventory.py` is unaffected (244); a POISON reader (`() => "POISONED\nPOISONED\n"`) is REJECTED by the S27/S28 content guards, falling back to the no-reader fragments with NO `"POISONED"` leak anywhere. EDIT-ORDERING CRUX: `restock` (added AFTER the run) replays on TOP of the renamed beacon and calls the NEW `check_quantity`/`insert_item`; `low_stock` (added BEFORE the run) references no rename-set name, so the script left it untouched (scoping each assertion to its own def block proves the ordering). KEPT-NAME hazard (S31/S32 pattern), PRESENT: terse-but-unrenamed `find_item` and `tot_value` SURVIVE as whole words (assert presence), and `tests/test_inventory.py` method names `test_qty_chk_less_than_stock`/`test_qty_chk_equal_to_stock`/… keep `qty_chk` as a SUBSTRING (`_`-bounded → `\bqty_chk\b` does NOT match them) so `\bqty_chk\b` count is 0 while the bare substring count is 5 — every absence assertion uses `\bname\b`, never bare `includes()`. The renamed test import surface is `from inventory import check_quantity, tot_value`. `extractFileEvents`: writes=3, edits=2 (`low_stock`+`restock`, both on inventory.py), userEdits=3 (`c67cfd9c` the coalesced script edit + `dbc2e4c1`/`f3e90535` the two run beacons), overwrites=0 (the synthetic completions are injected during per-file replay, not by `extractFileEvents`). Per-file (real reader): `inventory.py` [write,edit,userEdit,edit] → 4 revisions / 244 L (172→192→192→244), `tests/test_inventory.py` [write,userEdit,overwrite] → 3 / 79 L (79→51→79), `rename_inv.py` [write,userEdit,overwrite] → 3 / 45 L (43→17→45). The synthetic completion changeIds are backup blob names, kept out of the DAGs (spec 40) → fileDAG: `inventory.py` 4 nodes (B,D,G,I), `test_inventory.py` 2 (C,H), `rename_inv.py` 2 (E,F). Linear (one surviving branch, tip #72049b4a, three files, `rewound.length === 0`). 12 new tests (6 engine + 6 CLI); 487 green; S1–S34 + m1–m7 byte-for-byte unchanged.
[x] S36 -> [x] CSV-USER-EDIT-MCP script-driven RENAME — a CHARACTERIZATION/REGRESSION LOCK (NO `src/` change), the MCP-SANDBOX TWIN of S33: the SAME CSV-user-edit whole-word rename shape, but the rename driver runs through the context-mode MCP sandbox (`ctx_execute`) instead of the Bash tool. THE ONE THING that makes s36 distinct from S33: loading the JSONL depends on S32's ALREADY-SHIPPED parser fix — the MCP-run assistant records carry `attributionMcpServer:"plugin:context-mode:context-mode"` / `attributionMcpTool:"ctx_execute"` (7 such records), keys S32 added to the assistant allow-set in `src/parse/loadTranscript.ts`. With that fix present the engine clears s36 end-to-end with no new code. ONE `apply_renames.py` MCP run reads `renames.csv` and applies each `\bold\b`→`new` whole-word rename across `billing.py` and `tests/test_billing.py`. THE CSV ELEMENT (shared with S33): `renames.csv` is WRITTEN with a header + 3 rows (`calc_tot,calculate_total` / `fmt_money,format_currency` / `apply_disc,apply_discount`) then USER-EDITED to APPEND a 4th row `chk_stock,check_stock` BEFORE the run — so `renames.csv` reconstructs to a 2-revision ladder (rev 0 = 4 lines WITHOUT the 4th row; rev 1 = 5 lines WITH it). THE CSV USER-EDIT IS LOAD-BEARING: the post-script `reorder` Edit on `billing.py` calls `check_stock`, a name that exists ONLY because that manual 4th row flowed through the MCP run. Ladder: Write `billing.py` (terse `calc_tot`/`fmt_money`/`chk_stock`/`apply_disc` + helpers) + `tests/test_billing.py` → Edit `billing` +`validate()` (PRE-script; references no terse name, so the rename leaves it untouched) → Write `renames.csv` (3 rows) → User-edit `renames.csv` (append `chk_stock,check_stock` — a GENUINE manual `edited_text_file` edit, NOT a script beacon) → Write `apply_renames.py` (driver; whole-word STRING-CONCAT `re.sub(r"\b" + re.escape(old) + r"\b", new, text)` read from the CSV) then run it through the MCP sandbox (`ctx_execute`, NOT Bash; opaque, ZERO file events) → Edit `billing` +`reorder()` (POST-script; calls the NEW `check_stock` + `calculate_total`). The single run emits TWO COMPLETE `edited_text_file` beacons (uuids `de1f5023…` billing / `aecbb827…` test). THE HEADLINE LOCK: both beacons are COMPLETE, so the engine adopts the fully-renamed state FOR FREE — no rescue stage fires (`completeTruncatedBeacon` S27 / `completeElidedBeacons` S28 / `seedStaleEditBases` S19 all INERT), every file reader-INDEPENDENT, clean poison matrix. THREE user-edits, MIXED provenance: `de1f5023` (billing) + `aecbb827` (test) are the MCP-run beacons, `96b40a66` (renames.csv) is the manual CSV edit — the engine treats all three UNIFORMLY, which is why no engine change is needed. NO kept-name hazard (unlike S31/S32): `tests/test_billing.py` method names embed NO terse substring. WHOLE-WORD HAZARD — `apply_disc`: the renamed `apply_discount` contains `apply_disc` as a prefix substring 5× in final `billing.py`, so a bare `includes("apply_disc")` would WRONGLY report the old name present — every absence assertion uses `\bold\b` (count 0), never bare `includes()`. Prose hazard: the NEW names appear many times in `billing.py` docstrings (e.g. `reorder`'s `:func:\`check_stock\``) — absence assertions target OLD whole-word names ONLY. Per-file: `billing.py` [write,edit,userEdit,edit] → 4 revisions / 219 L (156→184→184→219), `tests/test_billing.py` [write,userEdit] → 2 revisions / 50 L (50→50), `renames.csv` [write,userEdit] → 2 revisions / 5 L (4→5), `apply_renames.py` [write] → 1 revision / 51 L; ALL reader-INDEPENDENT. `extractFileEvents`: writes=4, edits=2 (`validate`+`reorder`, both on billing.py), userEdits=3 (`96b40a66,aecbb827,de1f5023`), overwrites=0. Linear (one surviving branch, tip #a7ed17a3, four files, `rewound.length === 0`). 12 new tests (6 engine + 6 CLI); 499 green; S1–S35 + m1–m7 byte-for-byte unchanged.
[x] S37 -> [x] DRIVER-BACK-AND-FORTH-MCP script-driven RENAME — a CHARACTERIZATION/REGRESSION LOCK (NO `src/` change), the MCP-SANDBOX TWIN of S34 (as S36 is to S33): the SAME `ledger.py` driver-back-and-forth whole-word rename shape, but the rename driver runs through the context-mode MCP sandbox (`ctx_execute`) instead of the Bash tool. THE ONE THING that makes s37 distinct from S34: loading the JSONL depends on S32's ALREADY-SHIPPED parser fix — the MCP-run assistant records carry `attributionMcpServer:"plugin:context-mode:context-mode"` / `attributionMcpTool:"ctx_execute"` (6 such records), keys S32 added to the assistant allow-set in `src/parse/loadTranscript.ts` (were it reverted, `loadRecords(S37_JSONL)` would throw `UnmodeledFieldError`). With that fix present — and every rescue stage s37 leans on already shipped (S27 `completeTruncatedBeacon` / S28 `completeElidedBeacons` / S34 `outOfWindowEditSeed`) — the engine clears s37 end-to-end with no new code. ONE `apply_renames.py` MCP run reads `renames.csv` and applies four `\bold\b`→`new` whole-word renames (`add_entry→record_entry` / `rm_entry→remove_entry` / `tot_debits→total_debits` / `tot_credits→total_credits`) across `ledger.py` and `tests/test_ledger.py`. THE "BACK AND FORTH" (shared with S34): `renames.csv` is WRITTEN with header + 2 rows → manual user-edit appends a 3rd row `tot_debits,total_debits` (step-5 beacon `#22de8fa9`, 4 lines) → `apply_renames.py` is WRITTEN → manual user-edit appends a 4th row `tot_credits,total_credits` (step-7, 5 lines, NO BEACON — recovered from a file-history backup via the EXISTING S27 `completeTruncatedBeacon`) — the driver write sits BETWEEN the two manual CSV edits, and the last two renames exist ONLY because of those interleaved manual rows. Ladder: Write `ledger.py` (terse `add_entry`/`rm_entry`/`tot_debits`/`tot_credits`/`bal` + helpers, 119 L) + `tests/test_ledger.py` (69 L) → User-edit `ledger` prepend `# ledger module — managed by finance` (step-2 beacon `#d6a766e2`) → Edit `ledger` +`audit()` (PRE-script; its body uses the OLD `tot_debits`/`tot_credits`, renamed in place by the run) → Write `renames.csv` (2 rows) → User-edit `renames.csv` append `tot_debits,total_debits` (step-5 beacon) → Write `apply_renames.py` (driver; PRECOMPILED two-line `pattern = r"\b" + re.escape(old) + r"\b"` / `text = re.sub(pattern, new, text)` form, NOT s34's inline form) → User-edit `renames.csv` append `tot_credits,total_credits` (step-7, NO BEACON, via S27) → run `apply_renames.py` through the MCP sandbox (`ctx_execute`, NOT Bash; opaque, ZERO file events; renames BOTH sources) → User-edit `ledger` append `# names normalized via rename script` (step-9, NO BEACON) → Edit `ledger` +`report()` (POST-script; calls the NEW `record_entry`/`total_debits`). The single MCP run emits TWO `edited_text_file` beacons (ledger `#9022d09a` / test `#a4d3d115`), BOTH INCOMPLETE, so the EXISTING rescue stages complete them. MIXED reader-dependence (like S25/S35, NOT uniform like S34): `ledger.py` (151) and `apply_renames.py` (66) are reader-INDEPENDENT (same with or without a reader); `tests/test_ledger.py` (real 69 / no-reader 68, MCP-beacon completion) and `renames.csv` (real 5 / no-reader 4, drops the step-7 row) are reader-DEPENDENT. POISON CAVEAT (s34's T6 shape is WRONG for s37): with a poison reader the three GUARDED files stay clean (`test_ledger.py` 68, `renames.csv` 4, `apply_renames.py` 66, no `"POISONED"`), but `ledger.py`'s rescue path ACCEPTS the poison backup (collapses to 19, leaks `"POISONED"`) — a LATENT robustness gap in a rescue stage, NOT a correctness gap (real-reader AND no-reader both give 151), OUT OF SCOPE for this char-lock; the poison test asserts cleanliness ONLY on the three guarded files and documents the ledger.py exclusion. EDIT-ORDERING CRUX: `report` (added AFTER the run) replays on the renamed beacon and calls the NEW `record_entry`/`total_debits`; `audit` (added BEFORE the run) had its body RENAMED in place (now `total_debits`/`total_credits`); both reaching the renamed names proves the CSV edits flowed through the MCP run. WHOLE-WORD hazard: the new names recur in docstrings/prose — absence assertions target OLD whole-word names ONLY (`\badd_entry\b`/`\brm_entry\b`/`\btot_debits\b`/`\btot_credits\b`). NO rename-pair substring hazard (simpler than S36's `apply_disc`/`apply_discount`): no new name contains an old name as a `\bold\b` match. The renamed test import surface is `from ledger import record_entry, bal, remove_entry`. `extractFileEvents`: writes=4, edits=2 (`audit`+`report`, both on ledger.py), userEdits=4 (`22de8fa9,9022d09a,a4d3d115,d6a766e2` — the step-7 and step-9 appends left NO beacon, so they are NOT in the multiset), overwrites=0. Per-file (real reader): `ledger.py` [write,userEdit,edit,userEdit,edit] → 7 revisions / 151 L (119→9→120→137→137→138→151), `tests/test_ledger.py` [write,userEdit] → 3 / 69 L (69→68→69), `renames.csv` [write,userEdit] → 3 / 5 L (3→4→5, the 5th via S27), `apply_renames.py` [write] → 1 / 66 L. The synthetic completion/reseed changeIds are backup blob names, kept out of the DAGs (spec 40) → fileDAG: `ledger.py` 5 nodes (B,D,E,J,K), `test_ledger.py` 2 (C,I), `renames.csv` 2 (F,G), `apply_renames.py` 1 (H). Linear (one surviving branch, tip #b86404ef, four files, `rewound.length === 0`; prompt #ba6aa3a0). 12 new tests (6 engine + 6 CLI); 511 green; S1–S36 + m1–m7 byte-for-byte unchanged.
