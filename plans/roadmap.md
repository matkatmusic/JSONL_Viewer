[x] S1 -> [x] fix types (no primitives) -> [x] commit
[x] S2 -> [x] Read/Edit tools + StructuredPatchHunk + 3 attachment kinds + isMeta gate
[~] Per-line reconstruction engine -> [x] S1 slice (Write create -> rm delete, --verbose/--diff); [ ] Edit/observation/verdict
[x] S3 -> [x] cp copy lineage (CopyEvent + seed-from-source-at-copy-time + first-class copy entry; 3 independent histories)
[x] S4 -> [x] overwrite (2nd Write to a present file = replay-time presence distinction; first-class overwrite revision kind; render-list split; queue-operation record type added)
[x] S5 -> [x] bash redirect (`>>` append = carried-prefix + genesis-suffix revision; `>` overwrite = S4 reuse; content recovered from the file-history sidecar via injected BackupReader, snapshot paths resolved against cwd; replay-edit split; DOES_NOT_EXIST_YET sentinel; render-list verb renames)
[x] S6 -> [x] git mv rename (generalized parseMvPaths to accept `git mv`; cwd-relative rename paths resolved absolute via shared resolveAgainstCwd in new structures/path-resolve.ts leaf module; reuses S2 rename lineage + edit splice; no new event kind, no sidecar)
[x] S7 -> [x] conversation rewind / code restore (branch-aware reconstruction; rewind = a parentUuid fork named by the final last-prompt leafUuid; surviving branch is the engine default, rewound branches PRESERVED & retrievable like unmerged git branches; new reconstruction_branch.ts model + reconstruction_branches.ts core split off engine; reconstructBranches + CLI default-all-branches with --surviving/--list-branches/--branch; no new event/revision/per-line kind)
[x] S8 -> [x] repeated code-restore rewinds + final conversation-only rewind (surviving working tree resolved from file-history-snapshot tracked-set changes, not the final conversation head; new reconstruction_tree.ts walkers + reconstruction_worktree.ts owner detection; findSurvivingHead working-tree-aware; no-op for S1–S7; no new event/revision/per-line/container kind)
[x] S9 -> [x] code restore to older code with no post-edit (surviving working tree is the restored code; findWorkingTreeOwner detects change by content identity — carried-forward backupFileName — not the version counter, so post-restore refresh snapshots don't move the owner; reconstructAll(S9) is the restored write turn, reconstructBranches(S9) has zero rewound branches; refines S8 locked decision #3; single change in reconstruction_worktree.ts, no new module/event/revision/per-line kind, no CLI change)
[x] S10 -> [x] conversation-only rewind with no post-edit (the files written on the abandoned conversation branch stay on disk and ARE the surviving working tree; the read-only post-rewind head is a file-less tangent, so zero rewound branches and a plain single-branch list; no engine change — the isolated conversation-only-rewind case of spec 35, strict no-op for spec 36 since the post-rewind snapshots repeat the same version AND the same non-null backupFileName; locked by tests + spec 37; new tests/reconstruction_engine_s10.test.ts + tests/reconstruction_cli_s10.test.ts + 1 synthetic branch test, no new module/event/revision/per-line kind, no CLI change)
[x] S11 -> [x] code restore then post-rewind rewrite (surviving working tree is the rewritten `multiply` code, recovered from its Write events; the abandoned pre-restore `add` turn wrote files so it is preserved as ONE rewound branch forked at the root checkpoint — `## surviving` + `## rewound` headers, not a plain list; no engine change; locked by tests + spec 38; the complement of S9 — a real rewrite produces a NEW non-null backupFileName at a NEW version, so the content signature changes and the working-tree owner ADVANCES, where S9's null-bfn refresh did not)
[x] S12 -> [x] conversation-only rewind THEN post-rewind edit (first production-code fix + a two-DAG CLI). The surviving branch Edits files whose creating Write is off-branch (on the abandoned rewound branch the conv-only rewind left on disk), so the Edit replays against an empty base and the engine CRASHED in insertHunkAdditions; fixed by seedEditBaseFromBackup (prepend a synthetic Write seeded from the at-or-before file-history backup) + a total context-line branch (resolveContextLine). Adds a two-DAG render: --graphConvo (forks at the rewind, rewound-above-surviving, oldest-first) + --graphFile (linear per-file disk lineage), sharing one letter per turn; NO flags = both graphs, the new GLOBAL default that retires the `## surviving`/`## rewound` text default for every scenario. New modules reconstruction_graph.ts + reconstruction_graph_render.ts + reconstruction_labels.ts + BranchRole enum; specs 39+40; 12 default-view CLI tests rewritten to the graph output; 151 tests green.
[x] S13 -> [x] multi-edit code-restore-read (the S9 analog with an EDITED abandoned branch). A code-restore rewind forks at a system record into an abandoned branch that Reads+Edits scenario13.py to add `farewell` and a surviving branch that only Reads (disk rolled back to the `greet`-only content). The abandoned tip is named by NO last-prompt head (the rewind re-prompted from the fork point), so findConversationBranches orphaned the `farewell` Edit — the fileDAG showed it but the conversationDAG/--list-branches missed the rewound branch. Fix Part 1: discover the rewound branch STRUCTURALLY from the parentUuid fork (a record parenting ≥2 isGenuineUserPrompt children) via new reconstruction_tree.ts walkers (isGenuineUserPrompt/findPromptForkPoints/collectDescendantUuids/findDeepestPromptOrReply) + new leaf module reconstruction_fork.ts (findStructuralRewoundBranches, with a dedup guard that skips abandoned subtrees already holding a head-based tip — protects S7/S8/S11/S12; S9/S10 read-only branches auto-filter via divergingIds.size===0). Fix Part 2: keep a file-less surviving branch visible when a rewound branch exists so the conversationDAG renders the fork honestly (`(no file changes)` marker, empty branch sorts last). No new event/revision/per-line kind; --surviving content + fileDAG unchanged; 172 tests green; S1–S12 byte-for-byte unchanged.
[x] S14 -> [x] multi-edit conv-only-read (the conv-only twin of S13). Same write→edit→rewind→read shape, but the rewind is conversation-only (no `, code`), so disk KEEPS the abandoned `farewell` Edit. Two bugs hid the rewound branch and mis-named the surviving one: (1) findSurvivingHead's working-tree override mis-fired — conv-only leaves `farewell` on disk so findWorkingTreeOwner lands on the ABANDONED branch (`2f3a2bcb`), `finalChain.has(owner)===false`, and the override handed back the abandoned prompt `fadbe55d` as "surviving"; (2) the abandoned prompt `fadbe55d` is itself a `last-prompt` head in S14 (it was not in S13), so the head-based pass claimed it and `subtreeHoldsClaimedTip`'s `claimed.has(abandonedPrompt)` short-circuit blocked structural discovery of the real `farewell` tip `68f74356`. Fix Part 1 (reconstruction_branch.ts): only redirect to the working-tree owner when the final-head branch records NO file change of its own — new survivingBranchRecordsFileChange helper (`extractFileEvents(selectBranchRecords(records, finalHead)).length > 0`) keeps finalHead for S14 while S8/S9/S10 (file-less surviving branch) still redirect. Fix Part 2 (reconstruction_fork.ts): delete the `claimed.has(abandonedPrompt)` short-circuit — collectDescendantUuids excludes `start`, so S7/S8/S11/S12 (claimed head tip is a descendant) stay skipped, only S14's deeper structural tip is discovered. Output is byte-identical to S13 (rewound `#68f74356` above file-less surviving `#de63b23a`, fileDAG B write + D edit unchanged). 8 new tests (4 engine + 4 CLI); 180 tests green; S1–S13 byte-for-byte unchanged. NOTE: findConversationBranches yields the degenerate head-based `fadbe55d` branch too (harmless — no diverging file change, filtered by reconstructBranches), so the engine test asserts the two meaningful branches' presence rather than a raw length===2 (the plan's stated count; see implementation notes).
[x] S15 -> [x] user out-of-band edit then conv-only rewind — opens the user-edit family (S15–S23): a user edits scenario15.py directly on disk, recorded as an `edited_text_file` ATTACHMENT (not a tool_use), then a conv-only rewind abandons that branch for a Read-only branch. New `AttachmentPayloadType.edited_text_file` + `EventKind.userEdit` + leaf module reconstruction_user_edit.ts (`userEditEventFrom`/`stripLineNumberPrefixes`) turn the snapshot into a user-edit file event; the shipped S13 structural fork discovery + S14 surviving-head guard then surface the fork with no further branch/worktree change. CORRECTED a defect in the S15 plan: `edited_text_file` is a disk-snapshot ECHO the IDE emits on every write/read, present in the locked S5/S13 transcripts too (and 9 future scenarios) — not S15-only as the plan claimed. So a content-aware guard records the snapshot as a `user-edit` change ONLY when it differs from the file's current content (per the user's "reconstruct the change history 100%, attribution second" directive): reconstruction_replay.userEditChangesContent drops no-op echoes from content views; reconstruction_branches.collectAcceptedUserEditIds + extractRenderableEvents keep the two graphs in sync; the CLI passes its sidecar reader into renderGraphs so the fileDAG is content-correct for the bash-redirect lineage. 17 new tests; 197 green; S1–S14 byte-for-byte unchanged (zero user-edit turns); S15 shows the genuine edit as a rewound `user-edit` turn above a file-less surviving Read branch + its fileDAG turn.
[x] S16 -> [x] multi-edit code-restore then post-restore RE-EDIT (the re-edit twin of S13). The code restore (`Rewind: 2, code`) abandons the `farewell` edit and rolls scenario16.py back to greet-only; the surviving branch then re-edits to add `shout`. First scenario where a structurally-discovered rewound branch (the S13 mechanism — abandoned tip #24093c68 is named by no last-prompt head) coexists with a surviving branch that records its OWN file change — the working-tree owner is the `shout` snapshot 2de1cd62 on the surviving chain, so findSurvivingHead keeps the final head a4ec5565 via its on-branch short-circuit (reconstruction_branch.ts:48-49; the S14 survivingBranchRecordsFileChange guard a second line of defense). So the surviving conversationDAG branch shows its `E edit` (not the `(no file changes)` marker S13/S14/S15 used). The lone `edited_text_file` echo (record 80, greet-only) is dropped by the S15 content-aware guard, so no user-edit turn appears and the fileDAG kind column stays width 5. NO engine change — locked by characterization/regression tests (GREEN on arrival, like S10/S11). 9 new tests (4 engine + 5 CLI); 206 green; S1–S15 byte-for-byte unchanged.
[x] S17 -> [x] multi-edit conversation-only rewind then post-rewind RE-EDIT (the conv-only twin of S16). The conv-only rewind (`Rewind: 2`, no `code`) abandons the `farewell` edit CONVERSATIONALLY but leaves it on disk, so the surviving branch re-edits the still-farewell file to add `shout` — surviving working tree is greet + farewell + shout (KEEPS farewell, inverting S16's greet + shout). The abandoned `farewell` edit is the same structurally-discovered rewound branch as S16 (tip #07038b43, rewind @ #4a69b697); the working-tree owner #60cee518 is on the surviving chain so findSurvivingHead keeps the final head #e53225b5 via its on-branch short-circuit. The surviving `shout` edit anchors on farewell, which is off the surviving branch's greet-only base, so the spec-39 resolveContextLine born-path materialises it. No `edited_text_file` echo exists (no user-edit turn). NO engine change — locked by characterization/regression tests (GREEN on arrival). 9 new tests (4 engine + 5 CLI); 215 green; S1–S16 byte-for-byte unchanged.
[x] S18 -> [x] linear external USER-EDIT with NO rewind, then a post-edit RE-EDIT on top (the inversion of S15). The user edits scenario18.py out-of-band (`# user was here`); because the content differs from what was written, the S15 content-aware guard KEEPS it as a recorded `user-edit` change on the SURVIVING lineage (S15 stranded its edit on a rewound branch; S18 has no rewind). Claude's `farewell` edit then anchors on the user-edited base, so the surviving tree is `# user was here` + greet + farewell. FIRST scenario whose fileDAG shows a `user-edit` kind on the surviving lineage and whose `--surviving` view KEEPS the user edit. NO engine change — locked by characterization tests (GREEN on arrival, like S10/S11/S16/S17). 9 new tests (4 engine + 5 CLI); 224 green; S1–S17 byte-for-byte unchanged.
[x] S19 -> [x] multi-edit USER-EDIT then CONVERSATION-ONLY rewind, then a post-rewind RE-EDIT (the conv-rewind twin of S18). B writes `add`; the user edits scenario19.py out-of-band (`# user tweak`, D); Claude edits to add `subtract` anchored on the user content (E); the user rewinds (`Rewind: 2`, no `code`) to the original prompt; Claude then edits to add `multiply` (F) on the surviving branch. D (user-edit) and E (subtract) end up on the REWOUND branch (tip #7087c57e, rewind @ #b1368b53), but because the rewind is conversation-only it does NOT revert the file on disk, so F's `multiply` edit was computed by Claude Code against an advanced disk state (`add` + 2 blanks + `subtract` + `# user tweak`) that the surviving branch's events (B + F only) do not contain. FIRST scenario where a surviving-branch edit was computed against a disk state produced by OFF-branch edits — the divergence dropped the two blank lines between `add` and `subtract` (`insertHunkAdditions` slice on a too-short base). FIRST scenario since S12 needing a PRODUCTION-CODE change: `seedStaleEditBases`/`backupSeedWriteFor` generalise spec 39's edit-base seeding to a MID-stream edit whose hunk references lines past its reconstructed base — the surviving F-edit's base is reseeded from the file-history v3 backup so the surviving tree ends at the real 11-line on-disk file. Safe by construction (`editBaseIsStale` is false for every aligned edit, so S1–S18 are byte-for-byte unchanged). 9 new tests (4 engine + 5 CLI); 233 green; S1–S18 byte-for-byte unchanged.
[x] S20 -> [x] multi-edit USER-EDIT then CODE rewind, then a post-rewind RE-EDIT (the code-rewind twin of S19). B writes `add`; the user edits scenario20.py out-of-band (`# user tweak` on line 3, D); Claude edits to add `subtract` anchored on the user content (E); the user rewinds (`Rewind: 2, code`) to the original prompt; Claude reads the file then edits to add `multiply` (G) on the surviving branch. D (user-edit) and E (subtract) end up on the REWOUND branch (tip #51227411, rewind @ #e76a23d3), but unlike S19's conversation-only rewind the CODE rewind REVERTS the file on disk — and Claude Code re-writing the checkpoint surfaces as a synthetic `edited_text_file` (user-edit F) on the SURVIVING branch (tip #cacc87c7). The S15 content-aware guard records F, advancing the surviving base to the full 3-line disk state (`add` + `# user tweak`); G's `multiply` edit was computed against exactly that base, so its hunk is ALIGNED → `editBaseIsStale` is false → the S19 `seedStaleEditBases` reseed stays INERT. FIRST scenario with a `user-edit` on BOTH branches (D = the real human edit on rewound; F = the code-rewind restore echo on surviving). NO engine change — locked by characterization/regression tests (GREEN on arrival, like S10/S11/S16/S17/S18), with test 3 guarding that the S19 reseed does not mis-fire (a 4th overwrite revision would go RED). 9 new tests (4 engine + 5 CLI); 242 green; S1–S19 byte-for-byte unchanged.
[x] S21 -> [x] MULTIPLE out-of-band USER edits interleaved with Claude edits on a single LINEAR no-rewind branch (the multi-edit extension of S18). B writes `Counter`+`__init__`; the user inserts `increment` (D); Claude adds `decrement` anchored on D's line (E); the user inserts `reset` (F); Claude adds `get_count` anchored on F's line (G); the user PREPENDS `# Counter class` (H). All three user edits change content, so the S15 content-aware guard records each as a `user-edit` revision; the surviving scenario21.py is six revisions whose kinds alternate write→userEdit→edit→userEdit→edit→userEdit, ending at the real 8-line on-disk file. FIRST scenario with THREE `user-edit` changes coexisting on one surviving lineage, the FIRST where each Claude edit is anchored on content a USER edit produced, and the FIRST with a PREPENDING user edit (H shifts `get_count` to line 8). Because there is no rewind every edit base is aligned, so the S19 `seedStaleEditBases` reseed stays INERT (no synthetic seed revision) — the regression lock proving dormancy across many interleaved edits. NO engine change — locked by characterization tests (GREEN on arrival, like S10/S11/S16/S17/S18/S20). 9 new tests (4 engine + 5 CLI); 251 green; S1–S20 byte-for-byte unchanged.
[x] S22 -> [x] TWO out-of-band USER edits across a CONVERSATION-only rewind, one on each branch. B writes `Stack`+`__init__`+`self.items = []`; on the abandoned branch the user inserts `push` (D) and Claude adds `pop` (E); a conversation-only Rewind:2 reverts the chat to the root prompt but leaves push+pop on disk; on the surviving branch the user inserts `peek` (F) and Claude adds `is_empty` (G). Because the rewind is conversation-only, the surviving user-edit F's `edited_text_file` snapshot ABSORBS the off-branch push+pop, so the surviving scenario22.py is three revisions (write→userEdit→edit) where rev1 jumps 3→6 lines, ending at the real 7-line on-disk file; the rewound branch reconstructs separately to the 5-line init+push+pop. FIRST scenario with a USER edit on EACH side of a conversation rewind, and the FIRST where a surviving user-edit materialises off-branch edits that survived the rewind. The same user-edit kind adds ONE line on the abandoned branch (push) but THREE on the surviving branch (push+pop+peek), driven by per-branch disk state. The S19 `seedStaleEditBases` reseed stays INERT (F is a user-edit, not a reseed candidate; G's base is aligned) — verified because the NO-reader reconstruction is byte-identical. NO engine change — locked by characterization tests (GREEN on arrival, like S10/S11/S16/S17/S18/S20/S21). 9 new tests (4 engine + 5 CLI); 260 green; S1–S21 byte-for-byte unchanged.
[ ] S23 ->
[ ] M1 ->
[ ] M2 ->
[ ] M3 ->
[ ] M4 ->
[ ] M5 ->
[ ] M6 ->
[ ] M7 ->
