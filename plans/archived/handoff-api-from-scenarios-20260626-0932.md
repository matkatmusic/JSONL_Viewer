# Handoff: s37 engine gap is the script-execution-replay gap — port that approach from the JS engine to this TS engine
Conversation name: api-from-scenarios — close engine gaps via per-step coverage
JSONL: /Users/matkatmusicllc/.claude/projects/-Users-matkatmusicllc-Programming-RevEng-worktrees-api-from-scenarios/b5c8a619-0f19-4c55-ab98-ef5251ca9284.jsonl
Plan file: /Users/matkatmusicllc/Programming/RevEng-worktrees/api-from-scenarios/plans/goal-close-engine-gaps.md

## Branch
`api-from-scenarios` based on `master`. HEAD = `65de76b set goal for clearing engine gaps`. Working tree has **uncommitted** changes (mostly from a concurrent session): `M scripts/check_scenario_coverage.ts scripts/coverage_scenarios.ts src/reconstruction_reseed.ts tests/check_scenario_coverage.test.ts tests/fixtures.ts`, plus this session's `M plans/goal-close-engine-gaps.md scripts/coverage_ledger.ts` and untracked `?? plans/coverage-ledger.md scripts/coverage_sidecar.ts .DS_Store`. Nothing this session changed in `src/` survives — all engine experiments were reverted. There is no `-plate` branch.

## Goal
Drive the per-step coverage ledger to all-green: for every covered scenario in `scenarios/executed/` (a dir with a `.step_states/` ground truth), the TypeScript reconstruction engine must reproduce each captured step's touched files byte-for-byte. Work one failing scenario at a time, re-sweep all scenarios after each fix to prevent regressions, commit per fix. Full rules in `plans/goal-close-engine-gaps.md`.

## Current State
- **Coverage tooling exists and works.** `npx tsx scripts/check_scenario_coverage.ts [scenarioId]` runs the engine over scenarios and diffs each `.step_states/step-NNN/` folder; exit 0 ok / 1 gap / 2 unknown name. A concurrent session added multi-JSONL ingestion (`coverage_sidecar.ts`, `coverage_scenarios.ts`) so concurrent scenarios are in scope.
- **A timestamped ledger was added** (`scripts/coverage_ledger.ts` → `plans/coverage-ledger.md`) plus a guard test `tests/coverage_ledger.test.ts`. NOTE: the ledger output path was moved from `scenarios/coverage-ledger.md` to `plans/coverage-ledger.md` because `scenarios/` is a **symlink** to the Desktop RevEng project and is "beyond a symbolic link" for git.
- **The user is mid-rebuild of all ground truth.** They are re-capturing every scenario in a NEW per-step format: **one `.step_states/step-NNN/` folder per script step** (per user message / user action), including no-op steps that just re-capture the previous state. As of this handoff, **only `s37-script-rename-driver-back-and-forth-mcp` has been re-captured** (13 step folders, was 10); all other scenarios' `.step_states/` are temporarily ABSENT (`find scenarios/executed -maxdepth 2 -name .step_states -type d` returns 1). The full sweep is not runnable until the rest are regenerated.
- **s37 was diagnosed end-to-end.** Re-run + re-captured, it reproduces **12/13 steps**; the lone failure is **step-8** (the MCP-sandbox rename): engine emits an extra trailing line `# names normalized via rename script` (step-9's comment) at `ledger.py` line 124 that GT8 does not have.
- **`npx tsc --noEmit` was clean** at last check (before the user's regeneration churn).

## What Remains
1. **Wait for the user to finish re-capturing scenarios** in the new per-step format, then rebuild the baseline: `npx tsx scripts/coverage_ledger.ts` and read `plans/coverage-ledger.md`. Do not trust any sweep taken while `.step_states/` are mid-rebuild.
2. **Decide s37's fix path (the headline of this handoff).** s37's step-8 cannot be reconstructed from the captured transcript + file-history, because the MCP rename left no tool backup and the only post-rename backup already contains step-9's comment (no clean post-rename/pre-comment snapshot exists). Two cures were surfaced; the user is leaning toward the principled one:
   - **(Recommended) Port script-execution-replay into THIS TS engine.** Read `/Users/matkatmusicllc/Desktop/claude code src/RevEng/plans/spec-script-execution-replay.md` and `…/plan-script-execution-replay.md`. They specify modeling a script run (Bash OR `ctx_execute`/`ctx_batch_execute`) as a first-class **`scriptExecution` transform event**: recover the script + CSV (from disk or rebuilt from the transcript), derive the whole-token rename subs, **forward-validate** (`forward(pre) == expected post-script state`) with a count-assertion guard, then inject the transform at the run's timestamp so later edits replay over it. **These docs are written for a DIFFERENT engine (the JavaScript "Engine B" in the Desktop RevEng project, `api/reconstruct-file.js`, CommonJS) where it is already fully implemented — they must be PORTED into this TypeScript engine's pipeline** (`extractFileEvents` → the `completeElidedBeacons`/`seedStaleEditBases` stages → `replayEvents`). This is the general cure for the whole MCP/Bash-rename class, needs zero backups, and structurally prevents the step-9 bleed-through.
   - **(Alternative) Capture file-history per step in the runner.** The new format captures `.step_states` per step but NOT `~/.claude/file-history` backups per step. If the runner also snapshotted file-history at each step boundary, the existing `completeElidedBeacons` would reconstruct s37 unchanged. Simpler but a per-scenario band-aid, not the general fix.
3. **After s37 is green, continue the loop** at the next lowest-numbered FAIL. Pre-regeneration FAILs were: s37, s40, s42, s44, s56, s62, s72. Several may share the same script-replay root cause (the rename family) or may be capture artifacts — re-measure against the new captures before deep-diving.
4. **Decide the 3 scaffolding files** (`scripts/coverage_ledger.ts`, `plans/coverage-ledger.md`, `tests/coverage_ledger.test.ts`) plus the goal doc: they are an uncommitted clean baseline. Commit them once the user confirms, separate from any engine fix (commit-per-fix discipline).

## Key Files
- `plans/goal-close-engine-gaps.md` — the goal/loop spec (lowest-numbered FAIL first; re-sweep all after each fix; commit per fix; `.step_states` instruction-numbers vs engine snapshot indices are independent — match by content, not position).
- `scripts/check_scenario_coverage.ts` — the coverage tester (`checkScenario`, multi-JSONL via `jsonlPaths.flatMap`). Do NOT weaken its comparison; extending input handling is allowed.
- `scripts/coverage_scenarios.ts` / `scripts/coverage_sidecar.ts` — scenario discovery + file-history sidecar reader (multi-JSONL).
- `scripts/coverage_ledger.ts` → `plans/coverage-ledger.md` — the timestamped PASS/FAIL snapshot (re-sweeps, merges last-passing timestamps).
- `src/reconstruction_beacons.ts` — `completeElidedBeacons` (the stage that wrongly splices the comment-bearing backup into s37 step-8) and `completeTruncatedBeacon`; `backupIsWithinBound` is the `notAfter` bound.
- `src/reconstruction_reseed.ts` — `seedStaleEditBases` + the concurrent session's new `seedBeforeEdit` (uncommitted).
- `src/reconstruction_branches.ts` — `reconstructFileOver` (the pipeline order: extract → seedEditBaseFromBackup → completeElidedBeacons → seedStaleEditBases → completeTruncatedBeacon → replay). This is where a `scriptExecution` stage would wire in.
- `src/reconstruction_extract.ts` — `extractFileEvents` (raw per-file event list; detection of a `scriptExecution` event belongs near here).
- `/Users/matkatmusicllc/Desktop/claude code src/RevEng/plans/spec-script-execution-replay.md` and `…/plan-script-execution-replay.md` — the script-replay design to PORT (written for the JS engine).

## Context the Next Agent Won't Have
- **s37's step-8 is NOT a `.step_states` data error and NOT a mishandling bug.** GT8 (`record_entry`, no comment) is correct. The engine has no clean source for it: file-history holds only `{add_entry,no-comment}`@pre-rename and `{record_entry,+comment}`@post-step-9. The MCP rename emits no Edit/Write records and leaves no backup; the next backup already carries step-9's comment. Verified empirically by dumping `backupWritesFor(records, ledger.py)` — no `record_entry`-without-comment backup exists in either the old or the re-run JSONL.
- **The new capture format does NOT include per-step file-history.** Confirmed: re-captured s37 still fails step-8 identically. So `.step_states` granularity alone cannot fix this class — the fix is either script-replay (engine) or per-step file-history (runner).
- **Two engine experiments this session were tried and REVERTED** (don't repeat): (a) flipping `backupIsWithinBound` from `<=` to `<` — behavior-neutral on the corpus, no effect on s37; (b) the bound is the wrong lever anyway — tightening it just trades "extra comment line" for "incomplete window," still a miss, because no clean backup exists to complete the windowed rename echo.
- **Why the bound reaches the wrong backup (root mechanics, for whoever tries the engine route):** for the post-rename echo beacon, `notAfter` = the *next event in the list*, which is a much-later edit (the list is record-ordered, and a second simultaneous echo is positioned last), so the bound fails to exclude the comment-bearing backup. Instrumentation confirmed `next` = the late edit, not the adjacent echo.
- **The `.step_states` ground truth vs the engine's snapshots use independent numbering** — pass/fail is "some engine snapshot reproduces this folder byte-for-byte," never positional. A failing scenario can mean "no snapshot captured that exact intermediate state" (s37's case) rather than "wrong content."
- **Env gotchas:** test runner is `node --import tsx --test tests/*.test.ts` (NOT vitest). `rtk` aliases hijack bare `grep` — use `/usr/bin/grep`. `scenarios/` is a symlink to the Desktop RevEng project (shared across worktrees); files written under it can't be git-added here. A concurrent session has been editing this same worktree (its changes are the uncommitted `src/reconstruction_reseed.ts` etc.); coordinate before committing. Running any `tsx` script prints noisy `Debugger`/`inspector` lines on stderr — filter them.

## How to Verify
From `/Users/matkatmusicllc/Programming/RevEng-worktrees/api-from-scenarios`:
- `find scenarios/executed -maxdepth 2 -name .step_states -type d | wc -l` — how many scenarios are re-captured (1 = only s37 so far; wait for more before sweeping).
- `npx tsx scripts/check_scenario_coverage.ts s37` — s37's per-step diff (currently `FAIL 12/13`, step-8 extra comment line at `ledger.py:124`).
- `npx tsx scripts/coverage_ledger.ts` — rebuild `plans/coverage-ledger.md` (only meaningful once ground truth is regenerated).
- `npx tsc --noEmit` — must stay clean.
- `node --import tsx --test tests/coverage_ledger.test.ts` — the ledger merge-parse guard (3 tests).
