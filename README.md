# JSONL_Viewer
A tool for inspecting claude JSONL files, for the purpose of debugging them and understanding how the /rewind command works

The webapp and reconstruction engine live in the `jfred/` submodule (https://github.com/matkatmusic/jfred).
Run `git submodule update --init jfred`, then `npm install && npm run app` to serve it on port 7343.
