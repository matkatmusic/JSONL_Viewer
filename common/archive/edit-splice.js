// ARCHIVED (phase 6, 2026-06-15) — moved byte-identical to api/edit-splice.js.
// Role: applies a single old_string -> new_string edit operation to a
// line-belief object (applyEditToBelief).
// Every export now lives at api/edit-splice.js (the canonical home); the
// original body is preserved there and in git (develop-baseline). No live
// caller requires this path — kept only as a migration record.
