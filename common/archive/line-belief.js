// ARCHIVED (phase 6, 2026-06-15) — moved byte-identical to api/line-belief.js.
// Role: the per-line state tracker's IN-MEMORY belief model — belief
// construction, observation overlay, authored-change / snapshot-verify
// application, presumed-degradation, and summary/timeline cloning.
// Every export now lives at api/line-belief.js (the canonical home); the
// original body is preserved there and in git (develop-baseline). No live
// caller requires this path — kept only as a migration record.
