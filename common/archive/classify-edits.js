// TOMBSTONE (Phase 7, tool-suite migration). analyzeJSONL + classifyEdits moved
// to api/rewind-classification.js (Phase 1). The browser-only live bodies kept
// here for the viewer <script> tags are now superseded — the viewers load
// api/rewind-classification.js directly. Nothing live remains.
// Full prior content: git (develop-baseline).
