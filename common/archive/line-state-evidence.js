// ARCHIVED (phase 6, 2026-06-15) — moved byte-identical to
// api/line-state-evidence.js (the move that hit EXACTLY the 300-line cap).
// Role: evidence-reference construction + event materialization and excerpting
// for the per-line tracker (line splitting/spans, numbered/cat line entries,
// text/structured-patch/blob ref constructors, dereferencing, excerpts).
// Every export now lives at api/line-state-evidence.js (the canonical home);
// the original body is preserved there and in git (develop-baseline). No live
// caller requires this path — kept only as a migration record.
