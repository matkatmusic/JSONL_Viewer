## Plan prompt template
read `/Users/matkatmusicllc/Desktop/claude code src/RevEng/plans/roadmap-100-percent-reconstruction.md` to prepare for writing the plan for implementing item XX. 
Then read `~/.claude/guides/planning.md`
Then, monitor the `RevEng/plans/` folder for when the handoff document for item XW is created.  When the handoff document lands, create the plan for item XX.  When the plan is finished, write a handoff doc using the /jot:handoff-prompt skill.

## Implement prompt template
read `/Users/matkatmusicllc/Desktop/claude code src/RevEng/plans/roadmap-100-percent-reconstruction.md` to prepare for implementing item XX. 
Then read everything in `~/.claude/guides/`
Then, monitor the `RevEng/plans/` folder for when the handoff document for item XX is created.  When the handoff document lands, begin implementing item XX.  When the implementation of item XX is finished, write a handoff doc using the /jot:handoff-prompt skill.

---
read `/Users/matkatmusicllc/Desktop/claude code src/RevEng/plans/roadmap-100-percent-reconstruction.md` to prepare for writing the plan for implementing item 18. 
Then read `~/.claude/guides/planning.md`.
Then, monitor the `RevEng/plans/` folder for when the handoff document for item 17 is created.  When the handoff document lands, create the plan for item 18.  When the plan is finished, write a handoff doc using the /jot:handoff-prompt skill.


read `/Users/matkatmusicllc/Desktop/claude code src/RevEng/plans/roadmap-100-percent-reconstruction.md` to prepare for implementing item 18. 
Then read everything in `~/.claude/guides/`
Then, monitor the `RevEng/plans/` folder for when the handoff document for item 18 is created.  When the handoff document lands, begin implementing item 18 using the /jot:implement.  When the implementation of item 18 is finished, write a handoff doc using the /jot:handoff-prompt skill.