// TOMBSTONE (Phase 5 of the tool-suite migration). All of this module's code
// moved to api/reconstruction-reference-sources.js (gatherReferenceSources,
// chooseReferenceSource, distinctBasenames + the gathering helpers). It has no
// viewer script tag, so — unlike the common/ tombstones — it is archived now.
// Full prior content is on develop-baseline
// (git diff develop-baseline -- tools/probe-reference-sources.js).
