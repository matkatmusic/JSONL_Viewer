[
  {
    "taskNumber": 7,
    "title": "jot repo: `terminal_windowSizeBlocks(s)` enum refactor",
    "description": "7. jot repo: `terminal_windowSizeBlocks(s)` enum refactor** — DEFERRED UNTIL ALL other tasks are done.`/Users/matkatmusicllc/Programming/jot`\n  (separate repo); `grep -rn \"windowSizeBlocks\"`. Not a RevEng change."
  },
  {
    "taskNumber": 17,
    "title": "GitHub Pages demo tier",
    "description": "17. GitHub Pages demo tier** — canned ReconstructionDocument JSON + `webapp/` with a\n  static-data shim replacing `/api/*`. Deliberately deferred \"Phase 4, planned separately when\n  reached\" (handoffs 20260702-1434/1639, 20260703-1010/2144); needs its own plan when reached.",
    "handoffFilePaths": [
      "plans/archived/handoff-develop-20260702-1434.md",
      "plans/archived/handoff-develop-20260702-1639.md",
      "plans/archived/handoff-develop-20260703-1010.md",
      "plans/archived/handoff-develop-20260703-2144.md"
    ]
  },
  {
    "taskNumber": 56,
    "title": "Pre-baseline reconstruction question UI (engine efficiency)",
    "description": "56. Pre-baseline reconstruction question UI (engine efficiency)** — when a project has\n  a supplied base commit (item 46), the WebApp asks BEFORE rendering the timeline: \"Do you\n  want to reconstruct file states that precede the supplied baseline git commit?\" If the user\n  chooses No, the timeline's first shown step is the baseline commit (the engine may skip the\n  work the beacon supersedes); otherwise reconstruct as we currently do. User-specified\n  2026-07-09; deferred out of item 46's scope."
  },
  {
    "taskNumber": 63,
    "title": "GitHub Pages demo tier",
    "description": "63. GitHub Pages demo tier — publish JFReD static (cheap path)** — supersedes the\n  approach in item 17 for the FIRST public demo: `jfred/` + `api/` + `web-shared/` is\n  ALREADY a zero-server browser app (client-side JSONL load + legacy-engine reconstruction,\n  `jfred/jfred-load.js`), so Pages can host it as-is with the item-60 sample JSONL\n  preloaded. Label it honestly as the debugger demo running the frozen legacy `api/`\n  engine, not Engine B. Item 17 (canned ReconstructionDocument JSON + `webapp/` static-data\n  shim replacing `/api/*` — the full-fidelity Engine-B demo) stays open as the later,\n  bigger tier; do NOT build the shim for launch — the README GIF (item 62) covers what the\n  live webapp looks like."
  },
  {
    "taskNumber": 64,
    "title": "Public-repo CI workflow",
    "description": "64. Public-repo CI workflow** — no `.github/` exists anywhere today. One workflow in\n  the public repo: `npm ci`, `tsc --noEmit` (`npm run typecheck`), `npm test` — viable on a\n  bare clone only after item 59 makes the suite scenario-submodule-optional. Add\n  `npm run build:webapp` to catch webapp tsconfig breakage. Nothing else (no matrix, no\n  release automation) until the repo has users."
  },
  {
    "taskNumber": 83,
    "title": "See if actual /jot folder can be reconstructed",
    "description": "83. See if actual /jot folder can be reconstructed."
  },
  {
    "taskNumber": 96,
    "title": "Deferred browser verifications: items 78, 80, 84 and the unified-reconstruction fix",
    "description": "Browser checks deferred to the user's post-session pass, bundled: (a) item 78 — visual/headless verification of the timeline-build progress overlay against `#/project/-Users-matkatmusicllc-Programming-jot-backup/timeline`; threshold 500 / batch 100 are first-cut constants, tunable if the overlay feels too eager or the bar too coarse. (b) item 80 — confirm at the repro that a long console line wraps, and re-wraps after dragging `#split-lr` to narrow the console; if long lines STILL overflow, the cause is elsewhere and the item reopens. (c) item 84 — the `☐`/`☑` range toggle is a `<button>` inside `.rev-head`, which otherwise holds `<span>`s; may need a CSS nudge — visual verify. (d) unified-juggling-conway — glance that the file-history revision-content pane still looks right (`computeContentAtTime` now renders from the revision's own `lines`); click a chip → drawer shows the fetched file, pick a range + chip → diff renders; measure the cold-load timing win on the real `jot-backup` project via the `--resetSandboxMemo` run. (e) task 93 — the bare `/file/<path>` route (no /rev anchor) now opens revision #1 in DIFF mode instead of the old collapsed all-revisions list; if the collapsed-list overview mattered, say so — the rev-card list on the left is the intended replacement. (f) task 103 — a failed compound git command still produces a (badged) commit node resolving to the same instant as the retry (badge-don't-suppress decision); flag if the duplicate marker ever shows up as a visible artifact in the viewer.",
    "handoffFilePaths": [
      "plans/archived/implementation-notes-item78-timeline-build-progress.md",
      "plans/archived/implementation-notes-item80-console-word-wrap.md",
      "plans/archived/implementation-notes-item84-unify-bottom-pane.md",
      "plans/archived/implementation-notes-unified-juggling-conway.md",
      "plans/archived/implementation-notes-task93-retire-file-history-view.md",
      "plans/archived/implementation-notes-tasks-103-91-94.md"
    ]
  },
  {
    "taskNumber": 119,
    "title": "Partial reconstruction: surface recoverable file states even when full reconstruction fails",
    "description": "Add \"reconstruct up to...\" functionality even if the full reconstruction fails. The reconstruction engine should be able to provide at least SOME reconstructed file states from the JSONL files it finds, even if the entire session cannot be reconstructed. The engine and webapp need a way to reflect this reconstruction state so the user knows what file states were recoverable and what were not.\n\nScope (refined with user):\n- Failure modes to survive (all three): (1) parser-level crashes — unknown record types/keys in newer-CC JSONLs must not abort the load; skip the bad record, keep parsing, reconstruct from what loaded (today an unknown record type crashes the viewer before the consent dialog); (2) engine-stage failures — a reconstruction stage throwing mid-session keeps all file states computed up to that point; (3) missing-evidence gaps — sidecar backups / git repo / recorded tmpdir gone: reconstruct the revisions that don't need the missing evidence, mark the rest unrecoverable.\n- Granularity: per-revision. Each revision of each file is marked recovered or unrecoverable, with a reason (matches the per-step coverage-ledger mindset).\n- Webapp UI (chosen A+C from mockups in plans/partial-reconstruction-ui-mockups.html): session-level partial-reconstruction banner with counts; inline dashed gap rows in the timeline; dashed/grayed missing rev-cards in the revision stepper; plus a per-file coverage strip in the file list (one segment per revision, red = unrecoverable, click a red segment for a reason popover). Same engine output feeds both treatments."
  }
]
