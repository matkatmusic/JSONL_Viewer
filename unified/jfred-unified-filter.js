// Moved to common/jfred-filter.js — this re-export preserves backward compatibility.
export { onJsonlLoaded, activateFilter, nextMatch, prevMatch, getActiveFilter,
  configureFilter, bindFilterEvents } from '../common/jfred-filter.js';
